name: DISABLED - Agentic Transition Markers (runner-neutral process)

on:
  workflow_dispatch:

jobs:
  disabled:
    if: ${{ false }}
    runs-on: ubuntu-24.04
    steps:
      - run: echo "Disabled. Agentic transition state is persisted directly in GitHub PR/comments by the active role."
