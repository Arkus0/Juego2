name: DISABLED - Worker Handoff (runner-neutral process)

on:
  workflow_dispatch:

jobs:
  disabled:
    if: ${{ false }}
    runs-on: ubuntu-24.04
    steps:
      - run: echo "Disabled. Validate handoff through repository protocol and exact-SHA execution receipts."
