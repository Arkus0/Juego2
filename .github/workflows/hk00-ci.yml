name: DISABLED - HK00 hosted CI (use exact-SHA execution receipt)

on:
  workflow_dispatch:

jobs:
  disabled:
    if: ${{ false }}
    runs-on: ubuntu-24.04
    steps:
      - run: echo "Disabled. Run scripts/hk00-verify-exact-sha.sh on a compliant Worker/Reviewer/local execution substrate."
