name: DISABLED - Telegram Notify (runner-neutral process)

on:
  workflow_dispatch:

jobs:
  disabled:
    if: ${{ false }}
    runs-on: ubuntu-24.04
    steps:
      - run: echo "Disabled while Juego2 uses zero-budget runner-neutral execution. Telegram transport is optional notification infrastructure, never a workflow gate."
