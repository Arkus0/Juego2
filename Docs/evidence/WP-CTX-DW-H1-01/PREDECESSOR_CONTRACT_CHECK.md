# WP-CTX-DW-H1-01 PREDECESSOR_CONTRACT_CHECK

Worker baseline: `c87e6fb69b2b0bba3608cb3f77073779580aae8d` (`main`, 2026-09-24).

## Accepted predecessors and authority

- `WP-CTX-DW-GATE`: canonical PR #165; independently reviewed PASS on exact candidate `7912383c806171aea101907c05a3b008bc7a5e3b`; merge `8b7b7715c7365acfb94670b179f21668a0562534`.
- `WP-H1-04`: canonical PR #185; accepted exact candidate `8c6ffd61d17e832ed5b9f900e8c0f7d4e85bf5f5`; merge `4f172f7aa9e7c4a0909be046d095eac5477ad765`.
- Frozen H1-04 authority consumed by this derived projection:
  - `Unity/ArkusUnity/Assets/Arkus/H1/CatalogueMapping.json`, Git blob `922dbdffbe2f0a622acc2e153ca8b181427f6ce6`, schema `arkus.h1-catalogue-mapping@1`.
  - `Docs/evidence/WP-H1-04/SOURCE_ADOPTION.json`, Git blob `664e83e25269f345a248ce43410a28ed0a670750`, schema `arkus.h1-04-source-adoption@1`.

## Inherited guarantees consumed

`WP-CTX-DW-GATE` already establishes that DW is optional derived navigation, that CTX must source-open contradictions/negative claims, that lifecycle validity must be computed rather than fixture-owned, and that H1 product work remains source-first. `WP-H1-04` already owns Unity catalogue correctness, accepted logical identities, the effective Quaternius source baseline and its licensing/adoption decision. Those product guarantees are consumed here and are not re-proved.

The shared `Arkus.DesignWorld` kernel already owns generic fact/relation/provenance, normalization, independent-universe projection, stale provenance resolution and deterministic rebuild validation. This workpack uses those accepted generic contracts rather than adding H1 meaning to them.

## New guarantees owned here

This workpack owns the first real H1-04 → DW adapter and lifecycle: exact source freeze, independently reconstructed H1 authority universe, minimal versioned H1 projection schema, complete facts/relations/cardinality/provenance comparison, deterministic rebuild, stale/corruption rejection and a current projection identity suitable for CTX `USE` admission.

The independent oracle reconstructs the accepted catalogue/source universe directly from the two frozen H1-04 authority documents. It does not consume production projection records, projected counts or the production H1 parser.

## Intentionally not re-proved

No Unity execution is rerun. H1-04 inventory truth, native locator correctness, source licensing and effective editor behavior remain inherited. Generic DW/H0 semantics remain inherited unless effective evidence exposes a generic defect; this candidate introduces only adapter-owned H1 vocabulary.

## Reopen conditions

Reopen H1-04 only with concrete evidence that either frozen accepted authority document is false/inapplicable to the accepted candidate or cannot supply the identities/source evidence it claimed. Reopen CTX-DW-GATE only if its accepted routing/lifecycle boundary is shown inapplicable to a real projection. A defect limited to H1 projection parsing, completeness, provenance, staleness or adapter schema belongs to `WP-CTX-DW-H1-01`. A hypothetical predecessor defect or desire for duplicate proof is insufficient.
