# WP-DW-00 — foundational proof matrix

FOUNDATIONAL_PROOF_VERDICT: READY
UNRESOLVED_PROOF_OBLIGATIONS: 0
KNOWN_UNDETECTED_DEFECT_CLASSES: 0
TRUST_BOUNDARY: DW-00 owns the generic Design World authority/projection/rebuild/provenance seam and its use of accepted public H0 world-state/inspection surfaces; accepted source semantics, H0 correctness outside the consumed seam, .NET/Git/runner/hash infrastructure and downstream CITY/PA/CTX/Unity semantics are outside this claim.
PROOF_BUDGET_VERDICT: WITHIN_BUDGET

`READY` closes the repository-backed foundational proof matrix. Strict Worker challenge already repaired the public revision/hash query contract, Release solution registration, explicit neutral source-hash/mechanical-invariant proof and, after the first Ready transition exposed it, the missing canonical frozen-candidate verification route for `WP-DW-00`. `scripts/arkus-verify-exact-sha.sh` now routes DW-00 to `scripts/dw00-verify-exact-sha.sh`, which re-executes the complete DW observer and checks the foundational/evidence/frozen-metadata boundary. The focused observer also includes both DW-00 test classes, including the representative Juego2 probe.

Because this file is repository evidence, this matrix commit is the final repository/evidence mutation before freeze. The final handoff must therefore obtain a fresh exact-SHA observation, strict Worker pre-review and Ready/freeze validation on the resulting immutable HEAD. Those terminal readiness facts are persisted externally on the PR so proving them does not mutate and invalidate the SHA they certify.

## Independently defined universes / oracles

- **Neutral completeness universe:** `StaticDesignAuthorityUniverse` is constructed independently of `IDesignAuthorityReader`. `DesignWorldProjector.Build` must resolve every required id; omitting a reader definition while the universe remains complete throws `dw.provenance_missing`.
- **Projection coverage oracle:** validator compares independently supplied expected fact ids with actual projected fact ids; neither the projection nor its index can shrink the expected set.
- **Provenance oracle:** current authority bytes, unique anchor occurrence, full source digest and anchor digest are evaluated by the reader; an existing derived row cannot self-confirm trust.
- **Neutral mechanical invariant:** the independent fixture requires the exact public relation set `root --contains--> item-a` and `root --contains--> item-b`, not merely a reference count.
- **H0 surface oracle:** projected H0 object ids and DW extension subject ids must exactly equal projected fact ids; public object/reference queries are bound to the accepted current revision+content-hash guard; reverse product dependency is checked independently from H0 source/project files by the canonical observer.
- **Representative product probe:** exact accepted CITY programme bytes are read directly; it is not the neutral completeness universe.
- **Freeze verification oracle:** canonical observation and canonical frozen verification are separate registered routes. Freeze verification re-runs observation and then checks READY/zero foundational evidence plus exact frozen PR metadata; the CTX Worker-handoff lint independently validates the external exact-SHA pre-review comment.

## Acceptance / proof-obligation matrix

| Proof obligation | Claim / trust-boundary scope | Completeness argument | Positive evidence | Causal negative control / defect injection | Result | Residual risk |
|---|---|---|---|---|---|---|
| Stable projected identity, schema/rule version and typed fact/relation envelope | DW-owned generic consumer types | every expected id is independently enumerated; values and relations are normalized deterministically | `NeutralFixtureRebuildsDeterministicallyThroughPublicH0InspectionSurface` asserts the exact public `root→item-a/item-b` invariant; representative probe tests | identity mismatch / relation target outside universe are fail-closed in projector; omission test exercises missing expected id | GREEN | Full domain-specific schemas are downstream. |
| Provenance detects missing authority | DW source-adapter/projection seam | independent universe remains complete while reader entry is omitted | normal neutral + representative builds | `IndependentUniverseMakesMaterialProjectionOmissionCausallyRed` | GREEN | No full Markdown discovery claim. |
| Provenance detects stale authority | DW provenance trust decision | validation resolves each projected anchor against current authority bytes, not cached index | normal rebuild validates | `StaleAuthorityBytesInvalidatePreviouslyProjectedProvenance` | GREEN | Whole-document digest is conservative. |
| Provenance detects ambiguous authority | DW provenance trust decision | exact source anchor must occur exactly once | unique anchors validate | `AmbiguousAuthorityAnchorCannotValidateAsTrustworthy`; `DuplicateRepresentativeAnchorFailsClosedRatherThanTrustingAnIndexRow` | GREEN | Source formats requiring richer anchors can add provider-specific readers. |
| Identical authority + projection version rebuilds equally | DW derived-state/rebuild claim | deterministic sorted ids/fields/relations + length-prefixed normalization + canonical H0 serializer | neutral fixture uses a fresh reader, captures/compares source digest before/after, compares normalized text/digest/canonical H0 bytes and empty diff; representative rebuild/diff repeats on real source | hidden-derived-state case rebuilds with new reader/projector and no cached projection | GREEN | Trusted hash/runtime behavior remains outside claim. |
| Material authority change changes derived result | DW causal-change claim | fact fingerprints and projection digest include typed values/relations/provenance | normal equal rebuild | `MaterialAuthorityChangeChangesNormalizedProjectionAndDiff` changes the neutral source bytes and matching typed projection value together; digest and fact diff must change | GREEN | Semantic wisdom of a source change is not judged. |
| Projection rule/version change invalidates cached derived state | DW rule-version/staleness claim | expected projection version is supplied independently at validation and included in normalized projection | same-version validation | `ReusingDerivedStateAcrossProjectionRuleVersionIsCausallyRed` | GREEN | Migration policy is future work. |
| Derived DW state is disposable and cannot become authority | DW authority boundary | rebuild requires only source reader + independent universe + version; no index is input | normal build and representative probe | `DeletingAllDerivedStateStillRebuildsFromAuthorityOnly` | GREEN | Persistence/index implementation is not required in DW-00. |
| Authority source bytes/hash unchanged by projection/query/validation | DW source-authority preservation | neutral reader exposes the digest of the immutable source text and the product probe compares repository bytes | neutral fixture compares source digest before/after fresh rebuild; both representative probe tests compare actual source bytes | any source mutation would fail the corresponding equality | GREEN | External filesystem corruption is trusted-base/out of boundary. |
| No CITY/PA semantics enter H0; dependency remains downstream | H0 boundary consumed by DW | DesignWorld project references only `Arkus.Game.World`; canonical observer scans H0 dirs for reverse DesignWorld reference and generic consumer source for domain-specific implementation vocabulary | `DesignWorldIsAConsumerAndCannotBecomeAH0Dependency` | observer fails if reverse H0 dependency or CITY/PA-specific generic implementation vocabulary is introduced | GREEN | Domain payload values may naturally contain source-domain text; H0 semantics do not. |
| Same public surface is usable by later consumers | DW-to-H0 public seam | derived facts are materialized only as public `WorldState` objects/references/extensions; query uses accepted revision+hash-bound `WorldInspectionService` calls | neutral `Summary`/`QueryObjects`/exact `QueryReferences`, representative `QueryReferences` | stale/missing revision/hash remains rejected by inherited H0 contract rather than bypassed by DW | GREEN | Mutation of source authority is intentionally not exposed through DW. |
| Representative approved Juego2 shape fits owned boundary | foundational content-shape probe | exact accepted CITY row, bounded to identity/granularity/provenance; separate from neutral universe proof | `CONTENT_SHAPE_PROBE.md` + both DW-00 test classes in the focused observer, including equal real-source rebuild + empty diff | duplicate real-shape anchor fails; material diff and version-change controls remain independently causal in the neutral proof | GREEN | CITY semantic/invariant truth stays DW-01/02. |
| Exact-SHA canonical observation | execution evidence | `scripts/arkus-observe-exact-sha.sh` routes `WP-DW-00` to `scripts/dw00-observe-exact-sha.sh`; observer enforces exact clean SHA, locked restore, Release build, all focused DW-00 tests, dependency/domain guards and regression | prior complete candidate observation proved GREEN; fresh final-HEAD repetition is mandatory before Ready | exact-SHA mismatch/dirty state/locked graph/boundary/test failure makes command nonzero | GREEN repository proof; FINAL-HEAD RECONFIRM REQUIRED | Final-HEAD confirmation is a handoff gate, not an unresolved product proof class. |
| Exact-SHA frozen-candidate verification | final freeze evidence | `scripts/arkus-verify-exact-sha.sh` has an explicit `WP-DW-00` resolver/dispatch and `dw00-verify-exact-sha.sh` re-runs observation then checks foundational, probe, residual, predecessor and frozen metadata | route/verifier implemented after first Ready exposed the missing registration; exact final-HEAD Ready run must prove GREEN | missing route, stale/failing evidence, non-clean observation or mismatched frozen SHA makes verifier nonzero | IMPLEMENTED; FINAL-HEAD RECONFIRM REQUIRED | External pre-review issue-comment validity is independently owned by Worker handoff lint. |

## Named causal negative-conformance classes

1. **Self-shrinking projection completeness:** keep independent universe complete, omit material reader fact -> `dw.provenance_missing` RED.
2. **Stale source anchor/index row:** project old bytes, validate against changed bytes -> `dw.provenance_stale` RED.
3. **Ambiguous authority location:** duplicate anchor -> `dw.provenance_ambiguous` RED.
4. **Stale cached derived output after rule-version change:** validate old projection with new expected version -> `dw.projection_version_stale` RED.
5. **Hidden generated truth:** discard all derived output and instantiate a fresh reader/projector -> equal canonical rebuild required.
6. **Domain leakage / authority inversion through dependency graph:** reverse H0 dependency or CITY/PA-specific generic implementation vocabulary -> canonical observer RED.
7. **Material change invisibility:** change authority bytes and the corresponding projected typed value consistently -> projection digest and fact diff must change.
8. **Unregistered freeze verification:** Ready/freeze must dispatch through the exact WP verifier and fail rather than silently accepting an unregistered workpack; DW-00 is now explicitly registered in both observe and verify routers.

## Final closure rule

This matrix commit is the **last repository/evidence mutation** before freeze. After it lands, the Worker stops writers, reads the resulting exact 40-character HEAD, requires fresh canonical exact-SHA observation for that HEAD, inspects the complete baseline→final-HEAD diff and performs the strict Worker pre-review against that same immutable HEAD. The CLEAN readiness record is then updated externally and the PR may transition Ready. Ready is valid only if the CTX Worker-handoff lint and canonical Freeze exact-SHA validation both pass on that unchanged HEAD. Any later repository/evidence-byte mutation invalidates readiness and requires the cycle again.
