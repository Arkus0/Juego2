# WP-H1-10 residual risk

- No WAL/fsync/power-loss durability theorem, remote backup, distributed storage, cross-machine checkpoint portability (fingerprints include line-ending-sensitive bridge source bytes and machine-local editor identity) or cross-Unity-version migration. These are named non-claims of the WP.
- Reconstruction parity is the normalized effective observation. Rendered appearance, runtime behaviour and byte-identical Unity serialization are outside the claim.
- The normalization excludes only the generation-local GUID/local file id of generated derivative prefabs. If a future bridge change made another generated locator observable, parity would fail closed (drift), not pass falsely.
- Accepted H0 import/compare/provenance, the H1-03A lifecycle, H1-04 source identity and the H1-05..09 materializer/observer are consumed. Concrete contrary effective evidence reopens the causal predecessor instead of adding duplicate H1-10 checks.
- The effective CI stages call the accepted Unity worker entry points directly inside GameCI Editor processes (the H1-09 pattern); the .NET host -> Editor dispatch of those entries is the accepted H1-03A lifecycle and the public composition is covered by the transport and production-host tests.
