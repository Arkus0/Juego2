# WP-H1-11 — Residual risk and classified findings

Scope reminder: H1-11 is the broad representative real-source conformance challenge. It owns the selected-item manifest, the import/adaptation recipe, the representative end-to-end scenario and its diagnostics. It does not own production art, H2 content, CITY geometry or any generic bridge subsystem.

## Classified content-shape findings

| # | Finding | Classification | Disposition |
| --- | --- | --- | --- |
| F1 | The adopted Medieval Village MegaKit Source archive and UAL1 Source contain **no lamp, no tree and no standalone rock model** (the WP's allowed-scope list names them as examples). | Named future/residual decision (not a current-WP blocker, not a predecessor reopen). | H1-11 must not adopt a new source to fill the list and may not manufacture substitute art. The closest real shape classes present are exercised instead: `Prop_Vine1` (foliage/leaf material, top-hanging pivot), rock-trim material on `Wall_Plaster_Door_Flat` and `Prop_Chimney`. A lamp/tree/rock requirement returns as a new reviewed adoption under `DEPENDENCY_IP_POLICY.md` (ART/H2) if production needs it. Recorded in `REPRESENTATIVE_SLICE.json#absentFromAdoptedDistributions`. |
| F2 | Real Quaternius modules are multi-material meshes (2–3 slots); the accepted H1-07 renderer adapter binds exactly one material slot. | Out of boundary / accepted scope (not an H1-07 defect). | Multi-material real sources keep their complete imported bindings through prefab sources (checked slot-for-slot after reload). The single-slot catalogue override is exercised on the single-material real meshes (`Prop_Crate`, `Prop_WoodenFence_Single`) sharing one catalogue material. Per-slot material overrides are a future adapter-schema decision. |
| F3 | The H1-04 facade FBX came from the pack's `FBX (Unity)` folder and is pinned by its own content hash; it is not an entry of the pinned URP archive payload. The new representative items are entries of the pinned archive itself. | Out-of-boundary observation (H1-04 provenance is true: same adopted pack, content-hash pinned). | No reopen. H1-11 items carry the stronger archive-entry binding; the facade keeps its accepted H1-04 binding. |
| F5 | The accepted H1-04 sidecars are GUID-only, so Unity applies its serialized ModelImporter defaults: **no FBX file-unit conversion** (`useFileScale=false`). The facade's centimetre geometry therefore imports ×100 relative to the kit pieces, whose original upstream sidecars convert units (`useFileScale=1`). UAL1 is authored in metres and is unaffected. Found by the first effective import (Stage 0 bounds). | H1-11-owned import/scale compatibility requirement (in claim). | Made explicit per item (`REPRESENTATIVE_SLICE.json#items[].import`, derived from the sidecar bytes) and validated against the effective `ModelImporter` and mesh bounds. The accepted H1-04 import identity is not rewritten (no churn of the H1-04..H1-10 evidence); the scenario's Unity binding states the facade's 1% unit scale and Stage A/B require the facade and the kit wall to realize at the same world width. Normalizing the facade recipe is a future ART import decision. |
| F6 | Unity orders imported submesh material slots differently from FBX material-connection order (e.g. `Door_1_Flat`: FBX `WoodTrim, WindowGlass`; Unity `WindowGlass, WoodTrim`). | Import-recipe fact, not a defect. | The oracle compares the slot material multiset with the FBX material set; order is not a claimed property. The bridge preserves the imported order exactly (source vs realized slot equality after reload). |
| F7 | The first rigged humanoid used as a *prefab source*: the accepted H1-06 relationship universe (`CollectRelationships`) enumerated `MeshFilter`/`MeshRenderer` references only, so the humanoid's `SkinnedMeshRenderer` mesh and material references were outside the normalized observation that H1-09 reconciliation and H1-10 parity consume. | H1-11-owned rig-shape compatibility requirement (in claim; H1-06 is inapplicable to the skinned shape, not false for its accepted shapes). | Skinned mesh/material references are now recorded with the existing relationship kinds. Causal control: a skinned material rebound inside the published generation is reported on `civilian.walk`; the .NET stage asserts the observed rig relationships. Bone transforms remain internal hierarchy (checked by the Stage A/B inspection), not asset relationships. |
| F8 | Drawing the real slice in an Editor process (the supplementary capture) left the source materials it drew dirty, and the Editor's exit-time save rewrote them: the catalogue-pinned `SourceSlice/FacadeImportedMaterial.mat` plus the importer's nested `SourceSlice/Materials/*.mat`. The accepted H1-10 environment fingerprint caught it fail-closed at the next checkpoint capture (`checkpoint.source-rebound`, hosted run on `777bafb`). The non-rendering bridge stages (materialize, observe, rebuild, negatives) left no such write. | H1-11-owned (its capture stage caused it); the inherited H1-10 guarantee worked as specified. | The capture stage clears the dirty state of every loaded SourceSlice object after drawing, records what it discarded (`capture.json#discardedSourceEdits`), saves, and byte-compares the SourceSlice and published-generation trees around the capture. Every Unity stage re-verifies the mounted SourceSlice against the admitted hashes after its Editor has exited. The same Editor behaviour in an interactive session is recorded below as a residual. |
| F9 | Removing a selected source used by an *asset* node (`Prop_Crate` → `crate.stack`): the accepted H1-09 observer reports the node as `projection.source-wrong-type` rather than `source-missing`, because Unity nulls the dangling `MeshFilter` reference and the observer cannot tell a removed source from a non-mesh one at that point. Prefab nodes and replaced bytes are classified exactly (`source-missing` / `source-rebound`). | Out-of-boundary observation, not a predecessor reopen: H1-09's guarantee (complete managed-scope observation that names the drifted node) holds; only the code granularity is coarse. | The removal is still actionable and never substituted: reconciliation names `crate.stack`, materialize refuses with `projection.catalogue-snapshot-stale` without replacing the published generation, the live catalogue names `quaternius.medieval.asset.prop-crate` (`catalogue.stale-mapping`) and the public restore blocks with `checkpoint.source-missing`. A finer observer code for dangling asset-node meshes is a future H1 diagnostics decision. |
| F4 | UAL1 is imported with the accepted H1-04 GUID-only sidecar (Legacy/Generic rig); no Mecanim Humanoid avatar is configured. | Named future/residual decision. | H1-11 proves the humanoid rig shape (skinned mannequin, 65-bone armature preserved through import, derivative prefab and reload) and exact idle/walk/sit clip references. Mecanim Humanoid avatar/retargeting and playback behaviour are H2 animation production, explicitly excluded by the WP. |

## Residual risks (outside the claim)

- Unselected pack content, finished character clothing/wardrobe, production variants, bespoke Cantabrian architecture/props, missing gameplay animations, production LOD/performance, visual polish and final animation behaviour remain H2/ART/CITY production concerns.
- Interactive Editor use can re-serialize upstream `.mat` sources in `SourceSlice` the same way (F8) when the project is saved after the slice was drawn. The accepted H1-10 checkpoint then blocks restore with `checkpoint.source-rebound` (fail-closed, correct direction) and the vault remount restores the exact bytes; making upstream sources write-protected inside an interactive Editor is a future local-tooling decision, not claimed here.
- The supplementary capture shows the kit modules untextured (no kit texture file is in the admitted slice) and the humanoids in bind pose (edit-mode capture does not evaluate the Animator). Textured production materials and animation playback are ART/H2 scope.
- The rendered capture is supplementary and hosted-rendered; it is not a visual-quality or parity oracle. Pixel output across GPUs/drivers is not claimed.
- Import expectations use a declared, uniform conversion (FBX handedness flip, centimetre→metre unit scale, single-root collapse). A future Unity importer that changes these conventions is detected (Stage 0 fails) but is not predicted.
- Historical exact-SHA freeze verifiers of already accepted WPs (for example `scripts/h1-05-evidence-check.py`, `scripts/h1-04-public-conformance.py`, `scripts/ctx-dw-h1-0{1,2}-verify-exact-sha.sh`, `scripts/ctx-dw-h1-02-proof.py`) pin the pre-H1-11 catalogue counts/blobs/projection digest of their own frozen candidates (and several also enforce a baseline-diff scope). They remain bound to those candidates and are not re-run on later `main`; the live suites (Main Safety including the CTX-DW-H1 lifecycle/identity/selective-adoption tests, H1-07..H1-10 Unity workflows) carry the regression.
- CTX-DW-H1 derived projection: refreshed to the extended authority (`Docs/evidence/WP-CTX-DW-H1-01/LIFECYCLE.md`, digest `bc9af3fc…`, 294 facts) because its accepted lifecycle requires a new identity whenever the pinned authority blobs change. The CTX-DW-H1-02 selective-adoption dispositions classify capability classes that are unchanged by additional catalogue rows; its recorded evaluation identity stays historical.
- Cross-machine portability of the checkpoint environment fingerprint remains the accepted H1-10 residual.
