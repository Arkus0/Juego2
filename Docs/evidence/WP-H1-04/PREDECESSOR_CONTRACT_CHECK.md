# WP-H1-04 PREDECESSOR_CONTRACT_CHECK

Worker baseline: `f8e64422200e4da648f6a58751a81bdba21fc84f` (`main`, 2026-09-24).

## Accepted predecessor and authority

- Direct dependency `WP-H1-03A`: implementation PR #176, reviewed candidate `24d24526487af0d32e69799960f4251e44f0b5ad`, independent PASS review #5303016012, implementation merge `90428b803948820663abfebaa3fe21eb37596247`, DocSync PR #183 and `DOCSYNC_COMPLETE` merge `2744b1e1d1c548ccad001b770a5bbd074813f288`.
- Live GitHub PR #176, its exact PASS, the H1-03A workpack, `Docs/evidence/WP-H1-03A/PREDECESSOR_CONTRACT_CHECK.md`, the H1 bridge architecture and ADR-H1-001 were opened. No accepted H1-03A capsule is indexed, so this check uses the authoritative PR/evidence and binding architecture directly.
- Relevant transitive authority is consumed from accepted H1-01 logical catalogue-reference vocabulary, H1-02 pinned Unity 6000.3.24f1 project, H1-03 project-bound host admission, and HK01/HK03/HK07/HK08 public discovery/transport/bounded-read guarantees. `DEPENDENCY_IP_POLICY.md` (HK00A) governs the first source adoption.

## Inherited guarantees consumed

H1-03A supplies the public composed Editor invocation path, fixed launch profile, typed executor pairing, project lease, main-thread execution, result identity, lifecycle failures and status recovery. H1-03 admission binds the fixed project/workspace and capability grants. H1-02 supplies the effective Unity project/editor baseline. H1-01 distinguishes logical catalogue references from Unity locators. H0 composition, transports, pagination and canonical world hashing remain accepted authority. These guarantees are consumed, not re-proved here.

## New H1-04 guarantees

This workpack owns the scoped effective AssetDatabase/type inventory; versioned logical-ID mapping, catalogue entries, snapshots and fingerprints; bounded public discovery/get; deterministic missing, stale, duplicate, incompatible and provenance diagnostics; real Quaternius Source slice adoption; move/copy/delete/reimport evidence; independence from canonical `WorldState`; and public JSONL/MCP visibility through canonical composition.

## Reopen conditions

Reopen H1-01 only with evidence that its accepted logical reference shape cannot represent an admitted catalogue entry without changing public semantics. Reopen H1-03A only if the accepted composed/typed Editor seam cannot carry a catalogue operation under its published launch and result identity. Reopen H1-02 only if the pinned project/editor cannot import the exact approved source slice under its accepted project baseline. A defect in catalogue enumeration, mapping, provenance or pagination belongs to H1-04. A hypothetical predecessor defect or desire for duplicate proof is insufficient.
