# WP-H1-04 claim and proof matrix

FOUNDATIONAL_PROOF_VERDICT: READY
UNRESOLVED_PROOF_OBLIGATIONS: 0
KNOWN_UNDETECTED_DEFECT_CLASSES: 0
TRUST_BOUNDARY: The pinned Unity Editor/AssetDatabase/TypeCache, operating system, Git checkout, SHA-256 and .NET toolchain are trusted according to `FOUNDATIONAL_PROOF_STANDARD.md`. Arkus owns the reviewed immediate-file roots, six admitted kinds, mapping/adoption reconciliation, normalized catalogue, public capability definitions and evidence checks.
PROOF_BUDGET_VERDICT: WITHIN_BUDGET

The effective universe is **every admitted main asset/subasset** in the immediate files of `Assets/Arkus/H1/SourceSlice` and `Assets/Arkus/H1/CatalogueProof`, plus the effective Unity `Animator`, `MeshRenderer` and `Transform` serialized schemas from `TypeCache`. It is obtained from `AssetDatabase.GetAllAssetPaths`, `LoadAllAssetsAtPath`, `LoadMainAssetAtPath` and `TypeCache` before reading the mapping. Nested importer-generated `Materials/` files and arbitrary other project roots/types are outside the declared catalogue slice; effective dependency paths to those imported files remain visible on the source model entries. A removed mapping row cannot remove an effective inventory obligation. An extra matching file/subasset, missing approved file, changed content/import identity or duplicated mapping turns the catalogue red.

The approved local Source import is pinned by `SOURCE_ADOPTION.md`, `SOURCE_ADOPTION.json` and `scripts/h1-04-import-source.ps1`. The Source archive/license/selected file hashes are checked before copying into ignored project-local input. No upstream byte is changed. The accepted Source itself cannot define Arkus logical IDs or canonical state. The retained Unity project remains built-in render pipeline; the original Source URP material is truthfully incompatible, while Unity's source-derived Standard material is compatible for the bounded catalogue probe.

| Acceptance obligation | Positive/evaluated evidence | Causal negative conformance and result | Boundary / residual |
|---|---|---|---|
| Exact approved external Source identity, commercial terms and imported slice precede cataloguing | `SOURCE_ADOPTION.*`, verified archive/license/FBX/material/UAL1 hashes, `EFFECTIVE_VALIDATION.json` Source pins, and exact four-slice admission constants | Missing/altered license or substitute source content fails `catalogue.adoption-missing` in `H1CatalogueTests`, including a coordinated mapping/adoption substitution; import script rejects hash drift | No broad pack ingestion, licensing of unrelated assets or art production |
| Public bounded discovery describes admitted scenes, assets, prefabs, materials, animation clips and component schemas | `EFFECTIVE_INVENTORY.json`: 250 rows (1 scene, 2 assets, 2 prefabs, 2 materials, 240 clips, 3 schemas); entry type/name/dimensions/dependencies, source status and serialized component fields; 1–64 page limit | Page 65 and wrong snapshot token fail; missing effective row/map reconciliation fails; `PUBLIC_CONFORMANCE.json` proves both transports expose the three catalogue capabilities | Reviewed roots and types only; nested generated materials appear as dependency locators |
| Stable logical IDs remain separate from Unity paths/GUID/file IDs | Versioned `CatalogueMapping.json` and native locator fields; disposable move keeps original GUID/local ID and model test keeps `logicalId` while path/fingerprint changes | Copy receives a distinct real Unity GUID; extra effective copy fails `catalogue.unmapped-effective-entry`; duplicate logical/native mappings fail | Moving outside reviewed/adopted roots requires a new admission decision |
| Missing/duplicate/stale/incompatible states fail visibly | `H1CatalogueSnapshot.Build` reconciles full effective inventory with every mapping and exact content hash; public `resolve` accepts source prefab and rejects original URP material with `catalogue.incompatible-reference` | Delete produces 248 rows and missing source; unit controls reject stale row, removed manifest row, duplicate IDs, wrong source and incompatible reference | Arbitrary renderer appearance and full URP conversion are outside this catalogue claim |
| Unchanged effective inventory produces one normalized snapshot/fingerprint | Fresh JSONL/MCP calls and reimport give catalogue fingerprint `014d510bd8e9fe0d2754fed0075b3a81787e57a1717baed29e7e03f54295bd21`; disposable baseline/restored normalized inventory SHA-256 `c339019e00153074096622699cfd696c43580defaeda48032e2ad11f8425c0bc` | Reimport control asserts same complete normalized inventory; changed page snapshot token fails closed | Unity's documented import and file-ID behavior is trusted at pinned version |
| Catalogue change cannot mutate canonical `WorldState` or its hash | `H1CatalogueTests` computes `CanonicalWorldStateCodec` hash around catalogue construction; implementation has no WorldState write dependency | The test would detect a state/hash change in the exercised path; native locator fields are output evidence, never authored identity | H1-05 owns materialization effects and binding validation |
| Missing/incompatible logical reference is detectable before materialization | Public `get` exposes `compatible`; public `resolve` returns structured missing/kind/incompatible errors; real Source material is incompatible and source prefab resolves | Wrong kind, absent logical ID, stale mapping, and original URP material fail before any materialization endpoint exists | Future H1-05 must consume this resolver for its own truthful publication path |
| Capability inventory comes from canonical composition across transports | `H1CatalogueContract` definitions/routes/grants compose into the accepted H1-03A host; typed executor pairing is checked at bind; `H1UnityEditorLifecycleTransportTests` and `PUBLIC_CONFORMANCE.json` assert JSONL↔MCP discovery and normalized query/get/resolve results | Omitting a public catalogue route/executor makes composition or bind red; omitting a transport projection entry makes the transport discovery test red | Generic transport framing, main-thread lifecycle and host policy remain inherited H0/H1-03A guarantees |

## Effective local execution

`scripts/h1-04-local-evidence.ps1 -AssetsRoot C:\Juego2-Assets -ExpectedSha <40-char SHA>` is the canonical physical-local round. It verifies the owner Source hashes without writing upstream files, launches the exact installed Unity `6000.3.24f1 (4e7b9b5b6244)`, compares the effective AssetDatabase/type inventory with committed evidence, repeats move/copy/delete/reimport in a disposable Unity project copy, builds the current .NET host and calls the real catalogue through JSONL and MCP, then compares the normalized result and clean candidate state. The final run's exact-SHA receipt is a durable PR comment; the report files are repository evidence and intentionally do not contain a self-referential candidate SHA. `scripts/h1-04-verify-exact-sha.sh` verifies the committed proof and focused .NET/transport controls on the frozen SHA in hosted CI.

## Content-shape probe

The game-representative facade is the approved Quaternius `Wall_Plaster_Window_Wide_Flat.fbx`, observed both as mesh asset and model prefab. Its Unity-derived Standard material is a compatible source-derived material; the exact original Source `MI_Plaster.mat` is retained as a truthful incompatible material because this pinned project has no URP Shader Graph. `UAL1.fbx` contributes the civilian animation-clip shape, including `Armature|Idle_Paper`; the single repository-owned scene is a harness fixture, and effective Transform/MeshRenderer/Animator serialized fields expose component-schema shape. Query/get/resolve cover those identities without producing game content.

Classification: no current-WP omission was found after the component-field addition. The URP material conversion and visual quality are named future art/bridge decisions, and H1-05 must consume compatible logical references before publication. No concrete H1-01/H1-03A predecessor reopen condition fired. Full pack coverage, Cantabrian adaptation, wardrobe, render appearance, arbitrary component mutation, CITY/keeper content and H1-11 broad conformance remain out of this boundary.

## Residual-risk and proof-budget audit

The proof uses one bounded real Source slice, one fixture scene, one reviewed component-type set, a full effective inventory versus mapping/adoption reconciliation, focused unit defect controls, a disposable Unity lifecycle round and public transport calls. It does not recursively prove Unity, the OS, hash primitives or generic transport correctness already accepted upstream. The local Source root is private owner input, so hosted CI verifies committed exact-source fingerprints and evidence while the exact-SHA physical-local receipt proves the effective state. These are the material proof inputs for this workpack; no known in-boundary defect class remains without a red control.
