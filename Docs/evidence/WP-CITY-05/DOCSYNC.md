# WP-CITY-05 — Post-PASS DocSync

DOCSYNC_STATUS: **DOCSYNC_COMPLETE**  
MODE: **PROCESS_ONLY / DOCUMENTATION_ONLY**  
DATE: 2026-09-21

## Accepted result

- Frozen candidate SHA: `10d1528b0354b16a614fb10a3933a25b32f15f28`
- Independent Reviewer verdict: **PASS**
- Review: `#5267776704`
- PR: `#83`
- Merge commit: `47909a72eb6d38332f62e9c01426c8cd40e1863b`

## Accepted repair history

Cycle 0 candidate `6e8179828d14f009239463d732fba63ed8d8e446` failed independent review `#5266587982` because the first grammar could weaken CITY-02-required access relations during generic-family / functional-POI binding: `loc.calle.everyday_shop` could lose required service access and the casco bar's required semi-private layer could be replaced by an untyped vertical/court candidate.

The accepted cycle-1 candidate repairs that family causally in `Docs/production/CITY_ENVIRONMENT_GRAMMAR.md`: functional-POI binding is monotonic/fail-closed, every inherited required `public`, `service`, `private` and `semi-private` role remains required after binding, and spatial/use qualifiers such as `vertical`, `court`, `rear`, `staff` and `storage` cannot substitute for the required access role. All 23 A/B rows are checked in `Docs/evidence/WP-CITY-05/ACCESS_RELATION_CONFORMANCE.md`.

The accepted candidate also retains the Worker-found repairs for road-capable clear-width ambiguity and host-fit false greens. Every A/B place has a bounded CITY-05 host/site witness that fits the selected parcel/open-site grammar and inherited CITY-02 programme-envelope ceiling without replacing CITY-02's conservative caps/reserves.

## DocSync actions

1. Marked `WP-CITY-05` COMPLETE and recorded the accepted candidate, review and merge.
2. Advanced the CITY spine to `CITY-00 ✅ -> CITY-01 ✅ -> CITY-02 ✅ -> CITY-05 ✅ -> CITY-06`.
3. Updated the root workpack index so `WP-CITY-06` is the next dependency-valid CITY workpack.
4. Preserved `Docs/production/CITY_ENVIRONMENT_GRAMMAR.md`, `Docs/production/CITY_ENVIRONMENT_DISCOVERY_REQUIREMENTS.json` and all accepted predecessor semantic owners exactly as reviewed; this DocSync creates no new exterior, interior, runtime or bridge semantics.
5. Reconciled the residual boundary: CITY-05 closes the missing reusable exterior-grammar / host-fit planning step but leaves exact parcel placement/realized dimensions, detailed interiors/discovery, retained-seed selection, Unity realization, actual asset inventory/adoption, implemented discovery capability/schema, keeper realization, authoring-efficiency proof and runtime semantics to their already named downstream owners.

## Boundary

This DocSync is documentation-only. It does not reopen CITY-00/01/02, alter the accepted 23 A/B role sets, change T/S/M/L ceilings or reserve arithmetic, create exact parcel polygons, design CITY-06 rooms/discovery topology, select CITY-03, authorize CITY-04/07/08 LOCAL work, import assets, create a canonical Arkus catalogue/schema, or author Living World/runtime behaviour.

CITY-05's fail-closed conditions remain active: a later composition that cannot satisfy an inherited access role, host range, coverage posture, programme-envelope ceiling, route/access invariant or hard ordinary/quiet reserve must reopen the causal claim rather than being relaxed by DocSync.

## Next action

Next CITY workpack: `WP-CITY-06 — Interiors + layered discovery`.

`WP-CITY-06` is dependency-valid after this DocSync but is not active until a human starts its Worker.

`DOCSYNC_COMPLETE`
