# captures/ — historical (Codex iteration at `290c487`)

These images were rendered with the built-in pipeline from the geometry of commit `290c487`, before the STRUCTURAL checkpoint. They are retained as history of that iteration, and because `FRESH_AUTHOR_SMOKE_TEST.md` cites them. They are **not** current evidence: the geometry they show was measured with 569 structural failures (`UNITY_STRUCTURAL_AUDIT_BASELINE_290c487.json`).

Current evidence lives in:
- `structural_checkpoint/`: neutral-material STRUCTURAL views of digest `a7f8534f…`;
- `provisional_builtin/`: dressed built-in views of the same geometry, non-final and superseded by URP;
- the URP CANDIDATE folder, to be produced after `WP-H1-GATE`.
