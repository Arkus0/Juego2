# WP-PA-06 — Worker handoff navigation

Workpack: `WP-PA-06 — Memory & Consequences research`  
Execution: `REMOTE_RESEARCH`  
Baseline: `main@f220a049da0df5e08e74d962a57d97dea4c56320`  
Branch: `pa/wp-pa-06-memory-research`  
Date: 2026-09-24

The live canonical PR body and external exact-SHA Worker pre-review comment own final candidate identity and freeze metadata. This repository file does not embed its own eventual commit SHA.

## Candidate claim and surfaces

The PA-06 result proposes selected actor-accessible experiences as bounded causal witnesses for later actor-owned choices. It keeps current world truth, belief and relationship state with their accepted owners and requires active consequences to retain enough explanation after summary. Ordinary encounters do not each become biography; severe causes can persist, and repair can change their current relevance without erasing history or restoring the default town by fiat.

Review surfaces:

- `Docs/research/living-world/results/PA-06.md` — primary-source contrasts, ownership, semantic model, dispositions, future consumer and empirical boundaries;
- `Docs/evidence/WP-PA-06/MEMORY_CAUSAL_FIXTURES.md` — required positive, negative and continuity cases;
- `Docs/evidence/WP-PA-06/PREDECESSOR_CONTRACT_CHECK.md` and `WORKER_PLAN.md` — exact inherited boundary and scope.

## Independent Reviewer focus

Challenge whether the paired day-3 decision really differs only by a selected past experience; whether player-caused and off-screen cases require legitimate actor access; whether 100 routine interactions are bounded in stored state rather than hidden in UI; whether the causal minimum survives compaction/save-load without privileged rehydration; whether corrected belief or repaired relationship can be overwritten by memory; whether severe causes remain meaningful while normal recovery still works; whether every cited external design claim has a primary source; and whether the result claims runtime proof or numeric tuning it cannot have. The global capsule audit's unrelated HK-GATE mismatch is recorded as a navigation fallback, not used as semantic authority for PA-06.

`WORKER_PRE_REVIEW: CLEAN` is not recorded here; it will be persisted as an exact-SHA PR issue comment only after final exact-SHA preflight and strict pre-review. A fresh independent Reviewer is required.
