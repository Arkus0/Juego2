# WP-DW-05 — PREDECESSOR_CONTRACT_CHECK

Baseline main: `18d4aa33be526c65a53796aa305a56fe8df15d12`  
Direct dependency: `WP-DW-04`  
Dependency status: SATISFIED (`WP-DW-04` independent PASS `#5291762220`, merge `f4a2f145101bc735388b682b20a7913cd720cd33`, DocSync complete in `Docs/evidence/WP-DW-04/DOCSYNC.md`).

## Authoritative sources read

- `Docs/workpacks/DW/WP-DW-00.md` through `WP-DW-05.md`
- `Docs/evidence/WP-DW-00/DOCSYNC.md` through `WP-DW-04/DOCSYNC.md`
- `Docs/engineering/DW_DESIGN_WORLD_ARCHITECTURE.md`
- `src/Arkus.DesignWorld/DesignWorldContracts.cs`
- `src/Arkus.DesignWorld/DesignWorldProjection.cs`
- `src/Arkus.DesignWorld/Arkus.DesignWorld.csproj`
- `tests/Arkus.Harness.Tests/Dw00RepresentativeProbeBoundaryTests.cs`

## Inherited guarantees consumed, not re-proved wholesale

1. DW-00: generic typed fact/relation envelope, authority provenance, deterministic normalization, fail-closed provenance and clean rebuild over accepted H0 public world/extension surfaces.
2. DW-01: CITY semantic rules stay outside H0; real CITY invariants can be independently oracled and causally fail.
3. DW-02: complete declared CITY production-query universes and content-shape outputs remain source-owned/provenance-bearing and deterministic.
4. DW-03: PA-01..05 projection/query preserves material finding/disposition/evidence/fixture meaning and source-open provenance; future PA schema pressure remains explicitly residual.
5. DW-04: on its exact six-task campaign only, the DW route preserved the frozen required agent outcomes while reducing median injected source bytes; this is bounded evidence, not a general adoption decision.

## DW-05-owned guarantees

DW-05 owns the falsification stress, not another domain rollout. It must:

- run a deliberately non-CITY/non-PA neutral third-shape through already-accepted generic DW/H0 seams;
- exercise projection, typed query over the public projected representation, provenance validation and rebuild without widening or changing the generic public contract or H0 semantics;
- prove one causal invalid neutral mutation RED through the same generic validation/provenance path;
- audit generic DW/H0 contracts and dependency edges for CITY/PA leakage;
- reconcile every predecessor residual/limitation into exactly one causal classification/owner; and
- produce bounded H2 planning input without claiming universality, external packaging readiness or downstream candidate acceptance.

## Owner anti-bias interpretation

The third-shape may add fixture/schema/test code and probe-specific glue. New code is not itself adverse evidence. A material adverse result exists when the neutral shape cannot be expressed with the accepted generic semantics without widening/changing the generic public seam, adding a shared probe-motivated abstraction/capability, changing H0 semantics, depending on CITY/PA knowledge, or materially degrading provenance/rebuild. A bug that violates an already-promised capability is recorded/classified but is not automatically an architectural refutation.

## Reopen triggers

- Reopen DW-00 only if the neutral probe demonstrates that its accepted generic projection/provenance/rebuild guarantee is false or unusable without domain leakage.
- Reopen a later DW predecessor only if this stress test proves that predecessor's accepted guarantee false.
- H0 requires a separate reviewed causal delta only if an accepted generic public seam is demonstrably contradictory/insufficient; DW-05 itself may not patch it.
- Domain-only inconvenience, naming preference, probe fixture work or downstream ambition is not reopen evidence.

No predecessor is reopened at implementation start. The probe is intentionally allowed to change that conclusion if executable evidence warrants it.
