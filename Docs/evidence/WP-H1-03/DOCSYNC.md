# WP-H1-03 — Post-acceptance DocSync

DOCSYNC_STATUS: **DOCSYNC_COMPLETE**  
MODE: **PROCESS_ONLY / DOCUMENTATION_ONLY**  
DATE: 2026-09-24

## Accepted result

- Frozen candidate SHA: `6d78ebc07e48419e279d4a17b93eeb099307316b`
- Canonical PR: `#168`
- Final independent PASS review: `#5299906832`
- Implementation merge: `b52fe8f67bb74880af8ed2d734c1293869fd9f86`
- Exact-SHA Candidate Validation: Actions run `35956034910` GREEN
- H1-03 Unity Host Policy on the frozen SHA: Actions run `35955397660` GREEN
- Arkus Main Safety on the frozen SHA: Actions run `35955397574` GREEN
- Effective Unity evidence: exact Unity `6000.3.24f1 (4e7b9b5b6244)` on the accepted GitHub-hosted substrate, with reviewed logical-resource resolution, managed-root containment and `ExternalReversible` write/restore evidence.

## Accepted claim

H1-03 establishes the separate versioned `arkus.unity-host-policy@1` admission boundary beneath transports without weakening the accepted H0 path. The accepted H1 profile binds the bootstrapped Unity project identity/workspace, reviewed managed-root/resource/time grants, truthful `ReadOnly` / `ExternalReversible` metadata and closed logical-resource request schemas.

A raw composed contract containing Unity host effects still fails the public H0 projection path. H1 admission cannot mint a capability absent from canonical composition, JSONL/MCP cannot gain adapter-only Unity authority, and the admitted projection retains the exact workspace plus reviewed grant snapshot needed by H1-03A.

The effective Unity proof derives the project root from the running Editor, resolves only reviewed logical resources beneath `Assets/Arkus`, rejects ambient path/URL/runtime-type-shaped selectors, performs the bounded reversible effect and restores the workspace.

## DocSync actions

1. Marked `WP-H1-03` COMPLETE / ACCEPTED and recorded the exact candidate, independent PASS review, implementation merge and exact-SHA validation runs.
2. Updated the H1 track so `H1-00`, `H1-01`, `H1-02` and `H1-03` are accepted predecessor truth.
3. Advanced the default H1 sequence to `WP-H1-03A — Public Unity Editor execution seam + lifecycle` (`HYBRID / GITHUB_HOSTED_UNITY_ELIGIBLE`), now dependency-valid but still `NOT_STARTED` until explicitly started.
4. Removed the stale statement that H1-03A is blocked on future H1-03 acceptance.
5. Preserved H1-03A ownership of launch/main-thread/lifecycle/cancellation/crash/restart semantics and preserved H1-04+ catalogue/materialization/source boundaries.

## Boundary

This DocSync changes documentation state only. It does not modify the accepted H1-03 implementation, rerun Unity, broaden the admitted authority boundary, implement the H1-03A process seam, authorize H1-04, alter H0 semantics, or change CTX/DW/CITY/PA ownership.

## Next action

Next default H1 workpack: `WP-H1-03A — Public Unity Editor execution seam + lifecycle` (`HYBRID / GITHUB_HOSTED_UNITY_ELIGIBLE`).

It is dependency-valid from accepted H1-03 but remains `NOT_STARTED` until a human explicitly starts its Worker.

`DOCSYNC_COMPLETE`
