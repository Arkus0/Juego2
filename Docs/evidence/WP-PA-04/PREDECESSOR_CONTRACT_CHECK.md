# WP-PA-04 — PREDECESSOR_CONTRACT_CHECK

Workpack: `WP-PA-04`  
Direct predecessor: `WP-PA-03`  
Date: 2026-09-22  
Worker: ChatGPT GPT-5.6 Sol

`PREDECESSOR_CONTRACT_CHECK: COMPLETE`

## Accepted direct predecessor / exact evidence

`WP-PA-03 — Social Graph That Changes Behaviour` is accepted, merged and DocSync-complete:

- accepted candidate: `d216f32f0c82bf57c23a3c7ef0c4433cbcbdc927`;
- independent PASS review: `#5270421825`;
- implementation/research PR: `#100`;
- implementation merge: `2f3862b601b0521a6a3d5a57afe54f182d037e97`;
- DocSync PR: `#101`;
- DocSync candidate: `1b3005ac9118aae3043efa204c3d1d8127681f7d`;
- DocSync merge: `e388f6e3c9d42e89418bd8877ed36fcbcf6d2aaa`;
- canonical accepted finding: `Docs/research/living-world/results/PA-03.md`;
- completion evidence: `Docs/evidence/WP-PA-03/DOCSYNC.md` with `DOCSYNC_COMPLETE` and PA-04 named next.

No accepted-contract capsule is relied on as semantic authority for this material boundary. The Worker directly read the accepted PA-03 finding and DocSync because PA-04 consumes the private-third-party-relationship knowledge constraint.

## Inherited guarantees consumed by PA-04

1. **Actor-owned choice.** Belief/knowledge may influence a decision; it does not become a second chooser or omniscient director.
2. **Authorized information.** Canonical private third-party relationship truth is not ambient actor knowledge.
3. **Directed relationship semantics remain distinct.** A belief about a relation cannot rewrite the PA-03 canonical edge or imply reciprocity.
4. **Receiver ownership remains intact.** Communication may create receiver epistemic input; it may not preselect the receiver's optional response.
5. **Bounded discovery remains binding.** Knowledge-backed target/action discovery cannot hide global population/fact enumeration.
6. **Explainable semantic inputs remain required.** Material choices must expose named reasons rather than opaque authority.
7. **Bounded current provenance does not become biography.** PA-06 retains autobiographical memory/forgetting/compaction ownership.
8. **Shared causal world.** Player/NPC actions may create legitimate information asymmetry through normal owners; no privileged player-only knowledge universe is required.

Accepted PA-01/PA-02 guarantees are consumed transitively where PA-03 exports them: routine is not the chooser, actor-owned agency remains bounded/explainable, decision inputs must be authorized, and receiver choice remains receiver-owned.

## Guarantees newly owned by PA-04

PA-04 owns only the epistemic product contract needed for later consumers:

- canonical truth distinct from actor belief/access;
- ignorance as `UNKNOWN`, not false or canonical fallback;
- legitimate acquisition via perception, communication, accessed public sources and explicit authored/bounded inference seams;
- false, partial and stale belief persisting until an explicit epistemic cause revises it;
- compact immediate provenance and acquisition/revision time;
- secrets/concealment/deception as access/acquisition effects rather than omniscient flags;
- actor-safe queries segregated from privileged debug/truth comparison;
- shared epistemic state for behaviour/dialogue rather than duplicate `knows_X` authorities;
- same-world/different-belief causal counterfactual;
- privileged/debug-metadata leakage negative control;
- Playable Causal City witness/non-witness/later-allowed-source proof;
- future H4 notes and explicit deferred runtime/empirical proof.

## Guarantees intentionally consumed, not re-proved

This WP does not re-prove:

- PA-01 routine semantics;
- PA-02 actor-origin/chooser architecture, bounded target discovery, commitment/replanning or generic receiver ownership;
- PA-03 directed relationship dimensions, structural ties, obligations or their isolated behavioural counterfactuals.

PA-04 exercises only its new composition seam: actor-facing decisions may consume epistemic state only when that state is legitimately available to that actor.

## Concrete predecessor reopen condition

Reopen PA-02/03 only on concrete contradictory evidence that an inherited guarantee cannot hold on the effective PA-04 path, for example:

- the only viable knowledge-dependent lookup necessarily starts with global population/fact enumeration;
- a private third-party relationship belief cannot be represented without granting ambient access to PA-03 canonical truth;
- receiver-owned response semantics can only function if sender/director code selects the receiver answer.

A theoretical implementation possibility, future schema uncertainty or desire to duplicate accepted proof is **not** a reopen trigger.

## Result

No concrete contradiction was found. PA-04 may consume the accepted PA-03 boundary without reopening it, while adding only the epistemic guarantees named above.

`PREDECESSOR_CONTRACT_CHECK: PASS`
