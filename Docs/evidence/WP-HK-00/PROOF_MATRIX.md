# WP-HK-00 foundational proof matrix

Candidate family: `wp-hk-00-v4-sol-exclusive`  
Binding contract: `Docs/workpacks/HK/WP-HK-00.md`  
Binding proof standard: `Docs/engineering/FOUNDATIONAL_PROOF_STANDARD.md` v1.2

This matrix states the Worker completeness argument inside the finite trust boundary recorded in `TRUST_BOUNDARY_AUDIT.md`. It does not replace independent Reviewer duty.

| Proof obligation | Claim/trust-boundary scope | Completeness argument | Positive evidence | Negative control / attack | Result | Residual risk |
|---|---|---|---|---|---|---|
| Canonical product source has single ownership | Arkus-owned candidate tree; Git is trusted as tree/index authority | Git establishes the tracked C# universe; every production source maps to exactly one fixed project. Effective compiler/PDB observations are cross-checked against that ownership. | `inventory/sources.json`; compiler/PDB/source checks | `unowned-source-anywhere`, `cross-project-source-compile`, dropped/generated/external source attacks | Implemented; final evidence/CI pending | Does not prove a malicious Git implementation; that is trusted base. |
| Portable baseline is Unity-compatible and host/tests use pinned .NET 8 | Arkus project configuration; pinned .NET behavior trusted | Fixed contract + evaluated/effective properties require netstandard2.1/C#9 for portable projects and net8.0 for host/tests/proof. | `inventory/projects.json`; MSBuild/compiler properties | `fixed-policy-relaxed-in-build` | Implemented; final evidence/CI pending | Future compatibility changes require reviewed contract/ADR. |
| Portable kernel has no Unity/UnityEditor/DFU/game-content dependency coupling | Arkus product project/source/dependency boundary | Product packages/raw refs are constrained; engine/DFU identities are rejected; complete product-source diff remains boundary-only. | package/reference/compiler/assembly checks; source inventory | `engine-raw-reference`; `production-package-injected` (DI-D) | Implemented; final evidence/CI pending | “Game-content” is semantic; current tiny source set remains full-diff reviewable. |
| Dependency graph is directional and acyclic | Arkus fixed/evaluated project graph | Evaluated `ProjectReference` graph is compared against fixed direction and checked for cycles. Emitted-IL edge check is additional causal evidence. | `inventory/graph.json`; graph/assembly checks | `dependency-cycle-backedge`; `required-edge-made-decorative` (DI-D) | Implemented; final evidence/CI pending | Proves HK00 module boundary, not later semantic correctness. |
| Canonical build contains every required project | Canonical Arkus solution; normal `dotnet sln` behavior trusted | Git/fixed project universe is independent of solution membership; canonical solution membership must exactly match it. | project inventory; solution membership oracle | `extra-project-anywhere`, `missing-fixed-project`, `solution-omits-fixed-project` | Implemented; final evidence/CI pending | Non-canonical unsupported entrypoints are not product claims. |
| SDK/toolchain is pinned | Repository pin/config; SDK/runtime implementation trusted | `global.json` fixes SDK 8.0.425 with roll-forward disabled; CI selects/asserts SDK/runtime; Actions are SHA-pinned. | `global.json`; CI preflight | `toolchain-pin-relaxed` | Implemented; final CI pending | Does not prove integrity of a malicious/compromised pinned toolchain. |
| Test dependency closure is reproducible | Repository package config/lock; NuGet normal documented lock/restore behavior trusted | Direct test roots are centrally pinned; full transitive graph with content hashes is committed; fresh isolated observation must match and canonical restore uses locked mode. | `Directory.Packages.props`; `packages.lock.json`; `scripts/proof.sh` | `dependency-lock-drift` | Implemented; final CI pending | NuGet implementation/service integrity is trusted infrastructure. |
| Clean restore/build/test succeeds headlessly on Linux | Canonical build path; OS/runner infrastructure trusted | Exact candidate is restored, rebuilt and tested on Linux using pinned toolchain and locked dependency graph. | `scripts/proof.sh`; HK00 CI | pristine GREEN after attacks; `real-warning-is-error` | Exact-SHA GREEN pending | Runner availability/integrity is infrastructure risk. |
| Warnings are errors | Arkus project/compiler configuration | Evaluated property and effective compiler switches require warnings-as-errors. | MSBuild/compiler checks | `real-warning-is-error`; `fixed-policy-relaxed-in-build` | Implemented; final CI pending | Warning-set changes follow toolchain/version change. |
| CI fails closed when required project/test/proof material disappears | Arkus candidate contents | Required-file checks and project/solution universe make absence observable. | `FixedContract.RequiredFiles`; repository checks | `proof-project-deleted`; `missing-fixed-project` | Implemented; final CI pending | CI service itself remains trusted infrastructure. |
| Project/source inventories cannot self-shrink through candidate policy | Arkus-owned in-claim repository universe; Git trusted | Git candidate tree defines existence; fixed classification is separate from generated evidence/manifest data. | `inventory/projects.json`, `inventory/sources.json` | `legacy-manifest-cannot-shrink-universe`; terminal-entry DI-D attack | Implemented; final evidence pending | No recursive proof of Git internals. |
| Owned source is what compiler actually consumes | Arkus-owned source on canonical build; compiler behavior trusted | Actual compiler source inputs and PDB documents/checksums are compared with Git ownership; tracked candidate bytes are observed for stability. | compiler/PDB/hash oracles; normalized `compiler-args.json` | dropped/generated/external/mutated source attacks | Implemented; final evidence pending | Does not certify malicious compiler internals; it checks effective observations under trusted compiler semantics. |
| Realistic repo-controlled alternate channels cannot silently defeat canonical proof | Defence in depth around repository configuration; underlying MSBuild/compiler trusted | Build XML, solution/response surface, effective imports, compiler extensions/refs and output integrity add realistic false-green protection. | build/import/compiler/output checks | closure/test/external-authority/reference attacks | DEFENCE_IN_DEPTH implemented | These guards do not expand HK00 into exhaustive MSBuild/compiler security certification. |
| Candidate/evidence bind to one exact SHA | Arkus handoff/evidence; Git/CI infrastructure trusted | Candidate remains read-only; observations are generated under ignored artifacts; CI compares committed evidence and freeze records exact HEAD. | exact-SHA preflight; evidence drift checks; handoff metadata | candidate-tree/evidence drift mechanisms | Final evidence + CI + freeze pending | Fresh Reviewer independently rechecks frozen SHA. |

## Proof-budget conclusion

The proof grew significantly while repairing a real self-shrinking-universe class. The current v1.2 audit has bounded that work: the material acceptance claims are covered, existing extra guards are classified as defence in depth, and no further hostile-toolchain families will be added absent an in-claim acceptance defect.

`PROOF_BUDGET_VERDICT: WITHIN_BUDGET`
