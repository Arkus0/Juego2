# WP-HK-00 foundational proof matrix

Candidate family: `wp-hk-00-v2-chatgpt`  
Binding contract: `Docs/workpacks/HK/WP-HK-00.md`  
Binding proof standard: `Docs/engineering/FOUNDATIONAL_PROOF_STANDARD.md`

This matrix records the Worker completeness argument. It does not replace independent Reviewer duty.

| Proof obligation | Completeness argument | Positive evidence | Negative control / attack | Result | Residual risk |
|---|---|---|---|---|---|
| Canonical product source has single ownership; no copied/linked product implementation | Git establishes the repository-wide tracked `.cs` universe independently. Every tracked C# source maps to exactly one fixed project boundary. Evaluated `Compile`, effective compiler sources and PDB documents are cross-checked against that ownership. | `inventory/sources.json`; source/compiler/PDB oracles | `unowned-source-anywhere`; `cross-project-source-compile`; `source-dropped-at-build-time`; `external-source-injected-at-build-time`; `generated-product-source-injected` | PASS | Generated/untracked product sources are forbidden. Semantic intent of the tiny boundary-marker source set remains full-diff reviewable. |
| Portable libraries use the fixed compatibility baseline; host/tests use .NET 8 | Fixed contract enumerates project kind/TFM. Evaluated MSBuild properties and effective compiler `/langversion` must match. | `inventory/projects.json`; project-property and compiler-option checks | `fixed-policy-relaxed-in-build` | PASS | Future Unity compatibility changes require a reviewed contract/ADR. |
| No UnityEngine, UnityEditor, DFU or game-content dependency enters the portable kernel | Product package allowance is empty; raw refs are rejected; compiler and emitted-assembly refs are inspected for forbidden engine/DFU identities. Product-source inventory is complete and minimal. | static package/reference checks; effective compiler refs; assembly refs; `inventory/sources.json` | `engine-raw-reference`; `production-package-injected` | PASS | “Game-content” is semantic rather than a sound identifier class; current product-source diff is boundary-only and independently reviewable. |
| Dependency graph is complete, directional and acyclic | Fixed edges are compared to evaluated `ProjectReference` graph. Missing/extra edges and cycles fail. Required product edges must also exist in emitted IL so decorative references cannot satisfy the contract. | `inventory/graph.json`; graph and emitted assembly-reference checks | `dependency-cycle-backedge`; `required-edge-made-decorative` | PASS | Proves HK00 boundary causality, not future semantic correctness of later code. |
| Canonical solution includes every required project | Git/fixed project universe is independent of `.sln`; `dotnet sln Juego2.sln list` must equal the fixed project set. | solution membership oracle; `inventory/projects.json` | `solution-omits-fixed-project`; `extra-project-anywhere`; `missing-fixed-project` | PASS | Future alternate entrypoints must extend proof rather than bypass it. |
| SDK/toolchain and CI execution surface are pinned | `global.json` fixes SDK `8.0.425` with `rollForward: disable`; CI asserts exact SDK; workflow Actions are immutable SHAs and runner family is `ubuntu-24.04`. | `global.json`; `hk00-ci.yml`; CI preflight | `toolchain-pin-relaxed` | PASS | Hosted OS image servicing remains external infrastructure; SDK/action code is pinned under repository control. |
| Clean restore/build/test succeeds headlessly on Linux | CI restores, builds and tests the complete solution from exact candidate checkout under pinned SDK/Linux runner. | `scripts/proof.sh`; HK00 CI | every attack reconstructs baseline GREEN; `real-warning-is-error` | PASS | NuGet/network availability is operational infrastructure, not kernel semantics. |
| Warnings are errors | Fixed/evaluated `TreatWarningsAsErrors=true`; warning demotion/suppression is constrained; compiler requires `warnaserror+`. | MSBuild + compiler oracles | `real-warning-is-error`; `fixed-policy-relaxed-in-build` | PASS | Warning-set evolution requires explicit toolchain change. |
| CI fails closed if required project/test/proof/attack tooling disappears | Required-file contract is checked before build; solution membership independently covers projects. | `FixedContract.RequiredFiles`; repository oracle | `proof-project-deleted`; `missing-fixed-project` | PASS | GitHub Actions service itself is external; freeze requires exact-SHA jobs to finish green. |
| Project/source inventories are mechanical and cannot self-shrink | Universe is NUL-delimited Git index/physical checkout, not scan roots/manifest config. Fixed classification is separate and process capture preserves terminal entries. | `inventory/projects.json`; `inventory/sources.json`; repository oracle | `extra-project-anywhere`; `unowned-source-anywhere`; `legacy-manifest-cannot-shrink-universe`; `terminal-nul-inventory-entry` | PASS | Git is trusted as candidate-tree authority; index/worktree divergence is checked. |
| Owned source is what the compiler actually consumes | Evaluated `Compile` is defense-in-depth; actual compiler source inputs are checked against ownership; PDB documents/checksums prove compiled source identity; tracked bytes are hashed before/after effective build. | compiler + PDB/hash + tracked-byte oracles | `source-dropped-at-build-time`; `generated-product-source-injected`; `external-source-injected-at-build-time`; `tracked-source-mutated-during-build` | PASS | MSBuild/compiler are trusted pinned toolchain components; independent observations reduce shared-parser risk. |
| Build extensions cannot silently bypass static project checks | Explicit repository imports are rejected; `MSBuildAllProjects` constrains effective repository-owned import closure; compiler analyzers are checked statically and on actual command line. | `RepositoryClosureChecks`; `AdditionalChecks`; `LateBuildChecks` | `explicit-custom-build-import`; `auto-directory-build-target`; `custom-analyzer-source-generator`; `late-target-analyzer-source-generator` | PASS | Official selected SDK/reference-pack logic is trusted external toolchain; repo/NuGet/external extensions are not. |
| Candidate/evidence bind to one immutable SHA | CI checks out/verifies PR head; generated inventories/results must produce zero Git drift. Freeze records that same HEAD after all jobs are green. | exact-candidate preflight; committed evidence; PR freeze metadata | evidence drift fails dedicated CI steps; candidate-tree divergence fails proof | PASS only after final CI | Reviewer must independently re-check PR HEAD == Frozen candidate SHA. |

## Completeness conclusion

The central HK00 claim is not derived from a manifest-selected subset. Repository/project/source existence begins with the exact Git candidate; solution membership is independently checked; MSBuild evaluation/import closure observes build configuration; compiler arguments observe effective inputs; emitted assemblies/PDBs observe outputs; candidate-byte hashes detect build-time mutation. The 23 causal self-attacks exercise known material omission/bypass classes and require intended-oracle RED followed by pristine GREEN.
