# WP-HK-00 foundational proof matrix

Binding contract: `Docs/workpacks/HK/WP-HK-00.md`  
Binding proof standard: `Docs/engineering/FOUNDATIONAL_PROOF_STANDARD.md`  
Current Worker line: PR #8, descendant of `main` process baseline `dd4d2bdda69400d86fdba2156a93d15165662442`.

This matrix records the causal completeness argument. It is Worker evidence, never a substitute for fresh independent Reviewer falsification.

| Proof obligation | Completeness argument | Positive oracle/evidence | Causal negative control | Current state | Residual risk |
|---|---|---|---|---|---|
| All material build participants are inside the fixed HK00 boundary | Git is the repository universe; every tracked XML root `<Project>` must be a fixed project or one of two fixed policy files. Exactly one `.sln/.slnx/.slnf` entrypoint is permitted and solution membership must equal the fixed project set. | `BuildSurfaceChecks`, `RepositoryClosureChecks`, `projects.json` | extra project anywhere; missing fixed project; solution omission; auto/explicit build import; alternate build surface | DESIGN CLEAN; final exact-SHA execution pending | Malformed/non-participating files are not semantic participants; anything actually imported/built is re-observed by evaluated/effective oracles. |
| Canonical C# source has single ownership and cannot disappear from proof | NUL-delimited Git index supplies the tracked `.cs` universe independently; every source maps to exactly one fixed project. Evaluated Compile, actual compiler sources and PDB documents/checksums must agree. | `sources.json`; compiler args; portable PDB SHA-256 docs | unowned/duplicate source; static/effective source drop; generated/external source; tracked source mutation; terminal-NUL entry | DESIGN CLEAN; final exact-SHA execution pending | Git is candidate-tree authority; index/worktree divergence fails closed. |
| Portable boundary/toolchain is fixed | Portable projects are `netstandard2.1`, C# 9; host/tests/proof are net8.0. SDK `8.0.425`, runtime/ref pack `8.0.31`, roll-forward disabled. | evaluated MSBuild + compiler args + direct proof bootstrap | toolchain relaxation; fixed property relaxation; warning demotion/suppression | DESIGN CLEAN; final exact-SHA execution pending | Hosted OS servicing and upstream SDK bits are external infrastructure and are recorded separately. |
| Proof oracle cannot redefine itself through its own MSBuild project | The authoritative proof executable is bootstrapped directly with pinned `csc.dll` from every Git-tracked `tools/Arkus.HK00.Proof/*.cs`, not from project Compile items. Source bytes are hashed before/after bootstrap. | `scripts/build-proof-oracle.sh` | proof-project/build-surface mutations; proof deletion; source mutation | DESIGN CLEAN; final exact-SHA execution pending | The Worker-owned proof is not independent authority; independent Reviewer must reconstruct/challenge it. |
| Dependency graph is complete, directional, acyclic and causal | Fixed project edges are compared to evaluated ProjectReference graph; canonical assembly references must contain required product edges so decorative references cannot satisfy the contract. | `graph.json`; evaluated graph; PE metadata | cycle/back-edge; unknown/extra/missing edge; decorative required edge | DESIGN CLEAN; final exact-SHA execution pending | Proves HK00 boundary causality only, not later semantic correctness. |
| No engine/DFU/product-external dependency enters product kernel | Product PackageReference allowance is empty; project XML is closed; effective references are restricted to framework packs or exact declared Arkus dependency outputs; PE refs are inspected. | static package/ref checks; compiler reference authority; PE refs | engine/raw ref; production package; rogue compiler reference | DESIGN CLEAN; final exact-SHA execution pending | Semantic game content is not soundly identifiable by filenames; current complete product diff is boundary markers only. |
| Test/proof external authority is reproducible and explicit | Test dependency closure is committed as `packages.lock.json` with exact versions/contentHash. A fresh isolated restore is byte-compared to the lock, canonical restore runs locked, and package cache is isolated under ignored artifacts. | lock file; isolated restore; `ExternalAuthority` | dependency-lock drift; external build import; test analyzer injection | DESIGN CLEAN; final exact-SHA execution pending | NuGet service availability is operational; package content is trusted only through locked restore, not package names alone. |
| MSBuild extensions cannot introduce hidden semantics | Repository build XML is a positive closed surface; `.rsp` files and alternate solution entrypoints are rejected; canonical invocations use `-noAutoResponse`. Evaluated `MSBuildAllProjects` must resolve only to Arkus-fixed files, selected SDK/packs, generated `obj`, or exact locked test packages. | build-surface oracle + import closure | explicit/auto imports; external package/build import; response-file/entrypoint attacks | DESIGN CLEAN; final exact-SHA execution pending | Selected SDK/packs and exact locked test packages are external implementation dependencies, not Arkus semantic authority. |
| Every effective compiler input channel is observable/closed | All nine canonical projects are rebuilt with `ProvideCommandLineArgs=true`. Complete normalized C# command lines are inventoried. Sources, references, analyzers, analyzerconfig and input-bearing switches are causally classified; unknown input channels fail closed or create inventory drift. | `compiler-args.json`; `CompilerInputClosureChecks`; non-product/product effective checks | addmodule/link/resource/additionalfile; late analyzer; rogue reference; generated/external source | DESIGN CLEAN; final exact-SHA execution pending | New compiler switches cannot remain invisible because the full command line is evidence; policy must be extended if the pinned compiler introduces a new semantic input channel. |
| Produced binaries correspond to observed inputs | Portable PDB document hashes bind compiled sources; PE CodeView identity must match portable PDB identity. Multifile/netmodule and managed manifest-resource outputs are rejected. Required edges are verified in PE metadata. | `AssemblyFacts`, `OutputIntegrityChecks` | output substitution; source mutation; netmodule/resource/input-channel attacks | DESIGN CLEAN; final exact-SHA execution pending | Native PE resource presence is not itself semantic evidence and is not falsely required to be zero. |
| Candidate is immutable during proof and attacks cannot contaminate it | Positive proof writes observations only under ignored `artifacts/observed`; tracked bytes and Git state are checked. Self-attacks execute in a disposable Git copy and export only ignored observations/logs. | `proof.sh`; readonly attack driver; Git/hash checks | candidate mutation and evidence-drift failures | DESIGN CLEAN; final exact-SHA execution pending | External filesystem/service failures are fail-closed/unavailable, never PASS. |
| Evidence is bound to exact candidate | CI checks exact PR head/toolchain, regenerates observed inventories/results and requires byte-for-byte agreement with committed evidence before freeze. Freeze SHA must equal Ready PR HEAD. | HK00 CI + main-owned Worker Handoff + PR metadata | inventory/result drift; handoff mismatch | BLOCKED: GitHub-hosted runner currently not allocating jobs | Candidate controls its CI recipe, so green CI is necessary but not independent proof; fresh Reviewer is the independent authority. |

## Completeness position

The central claim is not obtained from a manifest-selected subset. Existence begins at the exact Git candidate; build participation is independently classified; evaluated MSBuild observes imports/configuration; actual `csc` command lines observe effective compiler inputs for all nine projects; PDB/PE observe outputs; package authority is exact and locked; and candidate mutation is checked separately.

The current design audit found no known material omission class after the repairs listed in `WORKER_PRE_REVIEW.md`. Runtime/evidence status remains **NOT READY** until a GitHub runner executes the final exact-SHA positive proof and all 37 causal RED→GREEN controls with zero evidence drift.
