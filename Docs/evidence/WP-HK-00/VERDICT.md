# WP-HK-00 Worker foundational verdict

FOUNDATIONAL_PROOF_VERDICT: READY
UNRESOLVED_PROOF_OBLIGATIONS: 0
KNOWN_UNDETECTED_DEFECT_CLASSES: 0
TRUST_BOUNDARY: `Docs/evidence/WP-HK-00/TRUST_BOUNDARY_AUDIT.md`
PROOF_BUDGET_VERDICT: WITHIN_BUDGET

## Worker statement

The implementation and reconciled evidence cover the material acceptance claims of `WP-HK-00` inside the finite trust boundary documented in `TRUST_BOUNDARY_AUDIT.md` and governed by `FOUNDATIONAL_PROOF_STANDARD.md` v1.2.

The independent universe is the Git candidate tree plus fixed Arkus classification; generated inventories cannot define away projects or tracked production source. Effective compiler/build observations, locked dependency closure and causal controls cover the canonical build path without claiming hostile subversion of the declared trusted base.

Observation run #104 on `56919bfd71fa5f28a370e7ba03a49d07b76e722a` produced foundational proof GREEN and exactly 37/37 unique causal negative controls GREEN. The reconciled evidence in this candidate contains the resulting source/compiler-input inventory and full attack summary.

`FOUNDATIONAL_PROOF_VERDICT: READY` is a proof/evidence readiness statement, not Reviewer PASS. Freeze is permitted only after this final evidence-bearing HEAD itself passes the exact-SHA verifier with zero committed-evidence drift and the PR records that exact HEAD as `Frozen candidate SHA`.

## Proof budget

The proof grew only while closing concrete acceptance/false-green classes, principally the self-shrinking universe and effective build-input boundaries. The latest import-closure repair replaced a fragile oracle rather than adding an unbounded new proof family. Existing extra guards are retained as defence in depth and no further hostile-toolchain hardening is justified by the HK00 claim.

`PROOF_BUDGET_VERDICT: WITHIN_BUDGET`

## Freeze conditions

Before Ready/FROZEN_FOR_REVIEW, the same exact HEAD must have:

- exact candidate/toolchain preflight GREEN;
- read-only foundational proof GREEN;
- committed inventory equal to observation, including `compiler-args.json`;
- all 37 causal controls GREEN with exact committed-summary equality;
- clean locked Linux restore/build/test;
- `WORKER_PRE_REVIEW: CLEAN`;
- no later branch mutation.

Independent Reviewer remains mandatory and must reconstruct and challenge the frozen candidate without treating this Worker verdict as authoritative.
