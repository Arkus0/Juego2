# WP-H1-08 foundational proof matrix

Claim: finite Unity diagnostic composition for the accepted H1 projection path. Trust boundary: the accepted H0/H1 predecessor validators and normal Unity Editor/AssetDatabase behavior; H1-08 owns their composition, phase truth, aggregation, publication gate and the new finite-transform predicate. `Docs/workpacks/H1/H1_REMAINING_COMPRESSION_AMENDMENT.md` forbids duplicate predecessor implementations.

| Obligation | Completeness boundary / oracle | Positive and negative evidence | Residual |
| --- | --- | --- | --- |
| Enumerable unique inventory and effective execution | Fixed invariant IDs are compared with public inventory and the exercised proposed/current/postflight routes, rather than trusting a self-declared registry alone. | `H1ProjectionValidationTests.Inventory_IsFiniteUniqueVersionedAndDeterministicallyOrdered`; exact H1-08 EditMode workflow; canonical exact-SHA verifier. | Future Unity invariants require an explicit workpack. |
| Stable actionable diagnostics and independent aggregation | Registered check actions compose accepted H1-04/05/06/07 validators; diagnostics sort by fixed order and stable semantic fields. | Reverse-order source/hierarchy negatives; invalid node plus independent hierarchy defect; content-shape material/hierarchy probe. | Unity warnings outside the inventory. |
| Truthful proposed preflight | The same structural preconditions guard public preflight and effective materialization; raw parent wire shape is checked before Unity string normalization. Accepted source and component validators remain causal authorities. | Incomplete path/GUID/local ID negatives; missing and null parent public-request negatives; materialize counterpart; H1-08 effective Unity gate. | No arbitrary custom component serialization. |
| Truthful current/effective validation | Current identity and graph/realization drift derive from the active manifest and effective observation; expected invalid active state returns a structured diagnostic. | Active A/caller B, manifest digest drift, malformed manifest, deleted active scene negatives. | Unrelated editor exceptions remain editor failures. |
| Finite-transform invariant and publication barrier | The finite predicate receives a deterministic non-finite sample through the real postflight check. Publication happens only after composed postflight succeeds. | Finite-value and non-finite-sample controls; prior active generation retained on postflight failure. | Visual/runtime transform behavior is outside this claim. |
| Public versioned current/proposed result and no H0 relabeling | Public `Execute` returns versioned Unity inventory/result; canonical H0 diagnostics retain HK05 ownership. | Public current/proposed EditMode tests and baseline H0 Main Safety suite. | Future transports are projections of accepted canonical contracts. |

Content-shape probe: `H1ProjectionContentShapeTests.StreetCorner_BadMaterialReferenceAndIndependentHierarchyDefect_AggregateActionably` uses the approved facade source and a disjoint hierarchy defect. The material-reference and hierarchy diagnostics are current-WP composition evidence; facade visual quality and gameplay are out of boundary. No predecessor reopen condition was observed.

Proof budget: the repair uses the existing composition and accepted validators. The bounded wire-shape reader exists only because Unity's JSON normalization otherwise erases a materialization precondition; it adds no new semantic authority or external dependency. Final exact-SHA gate identities and the foundational readiness verdict are recorded in the durable Worker pre-review comment after all candidate bytes are frozen.
