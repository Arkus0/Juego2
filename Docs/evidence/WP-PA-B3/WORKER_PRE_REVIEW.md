# WP-PA-B3 — WORKER PRE-REVIEW

Worker state: `FROZEN_FOR_REVIEW_CANDIDATE`  
Execution class: `RESEARCH_BATCH`  
Baseline: `main@86ed180925ca683d5e0ff7e8e1c6e1fb59ec86e0`  
Branch: `worker/pa-b3`  
Date: 2026-09-26

## 0. Scope reviewed

B3 contract:

- `Docs/workpacks/PA/WP-PA-B3.md`;
- unit specification `Docs/workpacks/PA/WP-PA-13.md`;
- batch amendment `Docs/workpacks/PA/PA_BATCH_EXECUTION_AMENDMENT.md`.

Candidate artefacts reviewed:

- `Docs/research/living-world/results/PA-13.md`;
- `Docs/evidence/WP-PA-B3/SIMULATION_CONTROL_FIXTURES.md`;
- `Docs/evidence/WP-PA-B3/H3_H4_IMPLEMENTATION_HANDOFF_INDEX.md`;
- `Docs/evidence/WP-PA-B3/PREDECESSOR_CONTRACT_CHECK.md`;
- this file.

No runtime/source/Unity/CITY/H2 asset file is modified by the candidate.

## 1. Dependency / baseline

`PASS`.

PA-B2 is accepted via PR `#240`, exact accepted candidate `6d05206884ac729e02cdcceb2edeecb43244b4bd`, independent PASS review `#5326015639`, merge `c886ac2712bd81718607153f9cae9394959bcdf5`, and binding B2 DocSync. The B3 branch was created from the resulting DocSync main commit `86ed180925ca683d5e0ff7e8e1c6e1fb59ec86e0`.

`PREDECESSOR_CONTRACT_CHECK.md` records the consumed PA-01..12 guarantees and B3 non-claims.

## 2. PA-13 deliverable checklist

### Canonical dossier

`PASS_CANDIDATE` — `results/PA-13.md` exists and is separate from the batch handoff evidence.

### Budget dimensions without invented final constants

`PASS_CANDIDATE`.

The result distinguishes at least:

- initiative/reconsideration;
- candidate/interaction scope;
- propagation/concurrency;
- persistence/trace/memory;
- fidelity/reconciliation;
- aggregate cost/overload;
- reservation/fairness.

It explicitly defers final numeric candidate/event/CPU/memory/save/population/timing thresholds.

### Cooldown / inertia / salience / event-cap / degradation strategy

`PASS_CANDIDATE`.

The candidate requires commitment/inertia/cooldown-like stability but does not select one algorithm; treats salience as routing/persistence pressure rather than truth authority; bounds chain/relay continuation; and defines truth-preserving overload/degradation and emergency kill-switch rules.

### FULL/ABSTRACT semantic continuity

`PASS_CANDIDATE`.

The candidate requires common semantic owners, explicit reconciliation and truthful failure/degradation if a cheaper representation cannot preserve required state.

### SV-1

`PASS_CANDIDATE`.

Preserved as a falsifiable future executable requirement with:

- `>=10,000` irrelevant distant actor negative control;
- fixed relevant source/scope;
- semantic-result invariance;
- enumeration/expensive-comparison bound;
- no whole-population fallback;
- dense-local variant that rejects unbounded all-pairs work hidden behind a spatial index;
- H3/H4/later-profile proof ownership.

### SV-2

`PASS_CANDIDATE`.

Preserved as a falsifiable future executable requirement where elapsed omitted micro-time scales by orders of magnitude while material summarized change stays constant. Generic reconciliation work may not grow with every omitted micro-tick. H3/H4/later-profile proof ownership is explicit.

### Clumsy vs deliberate chaos

`PASS_CANDIDATE`.

`SIMULATION_CONTROL_FIXTURES.md` contains a paired same-start clumsy-player versus persistent-destabiliser fixture. The first must remain broadly functional without erasing the mistake; the second may overcome recovery and produce a materially different state without control-layer reset.

### Constructive transformation

`PASS_CANDIDATE`.

A same-owner constructive fixture allows sustained service/activity/cooperation changes to form a materially different positive/reorganised equilibrium without a `GoodTown` semantic mode.

### Kill-switch/degradation truthfulness

`PASS_CANDIDATE`.

Emergency containment may reduce admission/new optional work or keep safer fidelity, but cannot erase already-caused state, reset belief/relationship/goal, clear owner state, fabricate completion/location or mark skipped work complete.

### Empirical residual list

`PASS_CANDIDATE`.

Final population, candidate caps, cadence, cooldowns, propagation, persistence, CPU/memory/save, salience, reconciliation thresholds and hardware targets are explicitly deferred.

## 3. Donor-remapped A11-1..8

| Oracle | Worker pre-review |
|---|---|
| A11-1 anti-thrashing | PASS_CANDIDATE — F13-01 |
| A11-2 deterministic replay | PASS_CANDIDATE — F13-02 |
| A11-3 decoy/load locality | PASS_CANDIDATE — F13-03/F13-04 + SV-1 |
| A11-4 FULL↔ABSTRACT continuity | PASS_CANDIDATE — F13-05/F13-06 + SV-2 |
| A11-5 controlled overload | PASS_CANDIDATE — F13-07 |
| A11-6 cascade cap | PASS_CANDIDATE — F13-08 |
| A11-7 authored reservation | PASS_CANDIDATE — F13-09 |
| A11-8 explain the budget | PASS_CANDIDATE — F13-10 |

Additional donor failure classes are explicitly retained: silent budget drop, priority starvation, decoy sensitivity, catch-up burst, non-reproducible ordering and degradation changing truth.

## 4. PA-13 hard-negative audit

Candidate explicitly rejects:

- direct control-layer writes to beliefs/relationships/goals;
- automatic return to starting town under sustained causes;
- accidental butterfly apocalypse;
- global-scan fallback;
- unbounded spatial-bucket all-pairs work;
- ABSTRACT omitted-tick debt;
- silent dropped work;
- magic teleport/fabricated completion;
- truth substitution for actor ignorance;
- trace/reason amnesia;
- unbounded “important” exception lists;
- story-caused history reset/authority theft;
- hidden nondeterministic contention;
- guessed numeric budgets presented as validated;
- H2 presentation proxies presented as Living World proof.

`NEGATIVE_GATE_AUDIT: CLEAN`

## 5. H3/H4 handoff audit

`PASS_CANDIDATE`.

The handoff is traceable row-by-row to PA-01..13 result artefacts and separates:

- semantic requirement;
- likely first consumer;
- deferred empirical/runtime proof.

It does not define final APIs/classes/scheduler/schema and does not absorb PA-14.

The handoff assigns:

- H3: persistent embodied substrate, real opportunities/capacity, first bounded local-query proof and first fidelity proof only if that seam exists;
- H4: deeper actor/social/epistemic/memory/material/event/governance causal composition and PA-13 control/reconciliation;
- later representative profiling: numeric thresholds and target-hardware budgets.

## 6. H2 boundary audit

`PASS_CANDIDATE`.

Both PA-13 and the handoff explicitly state that H2 idle/walk/bounded movement/minimal bark characters are presentation proxies only and do not satisfy PA Living World or SV-1/SV-2 runtime proof.

PA-14 remains deferred until accepted PA-B3 + accepted H2-GATE.

## 7. Architecture / scope-creep audit

`CLEAN`.

Candidate does not select:

- Utility AI / GOAP / behaviour tree / rule engine;
- ECS / GameObject / hybrid representation;
- scheduler topology;
- spatial-index implementation;
- persistence schema;
- FULL/ABSTRACT representation;
- final GameFlow architecture;
- final H3/H4 workpack topology;
- shipping numeric budgets.

The only implementation-facing statements are semantic obligations and future executable owner assignments required by the B3 contract.

## 8. Change-surface audit

Pre-freeze comparison against the B3 baseline contains documentation/research evidence only:

- canonical PA-13 result;
- B3 fixtures;
- H3/H4 handoff index;
- predecessor check;
- worker pre-review.

No accepted PA-01..12 result is modified. No runtime code is modified. No H2/CITY/H1 workpack is modified.

## 9. Reviewer attack surface

Independent Reviewer should try to prove a concrete false PASS in these areas:

1. can simulation control ever alter predecessor semantics merely to save work?
2. can `SV-1` still hide a global scan or dense-local all-pairs surface?
3. can `SV-2` still accumulate work proportional to omitted micro-ticks with material change fixed?
4. can overload/kill-switch silently erase truth, actor state or causal lineage?
5. does anti-thrashing accidentally prevent valid response to material input changes?
6. can propagation caps become epistemic evidence or reset causal history?
7. can a low-severity isolated action cascade to collapse with no reinforcement?
8. can sustained destructive or constructive causes still be forced back to default town by PA-13?
9. does the H3/H4 handoff accidentally invent architecture or treat H2 proxies as proof?
10. is any numeric budget claimed as validated without representative runtime evidence?

Under `RESEARCH_BATCH`, broader architectural preferences or extra edge cases should be residuals unless they demonstrate a mandatory-contract false PASS or accepted-predecessor contradiction.

## 10. Worker conclusion

`WORKER_PRE_REVIEW: CLEAN`

`UNRESOLVED_MATERIAL_FINDINGS: 0`

The candidate is ready to freeze on one exact SHA and hand to an independent Reviewer. Worker does not assert final PASS.
