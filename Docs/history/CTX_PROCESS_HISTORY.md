# CTX process history

Status: **HISTORY / NON-AUTHORITATIVE NAVIGATION**

This file preserves CTX programme review/repair lineage that is useful for audit but should not become a normal Worker/Reviewer bootstrap read. Current eligibility, acceptance and transient PR/check state continue to come from live GitHub plus exact workpack/accepted evidence. This history cannot override them.

## Programme plan

- CTX plan PR `#102` — accepted candidate `c9ff3605048e05fded4d58a1de27450661d9fe0e`, independent PASS review `#5270686380`, merge `f3c8362b3d76fd4f78107d8142e07e476985f973`.
- DocSync PR `#103` registered the accepted sequence `CTX-01 -> CTX-02 -> CTX-03`.

## CTX-01

The first CTX-01 candidate was later corrected because its persisted freshness rule was self-referential. The accepted repair is PR `#110`, candidate `ea92e4eab36566ab3d0367fef64fefc0b2b0ff39`, independent PASS review `#5273801466`, merge `fbd3e5526e760efc89f54e7c12a274af10d4765f`.

A separate mechanical handoff incident on the same frozen bytes produced review `#5273364796`: canonical Ready metadata/process mode were missing and Ready-state checks were red. Repository bytes did not need to change; the metadata was repaired and gates rerun. CTX-03 classifies this family as `REVIEW_BLOCKED`, not as a semantic WP defect, so future Reviewer rounds are not spent discovering derivable lifecycle metadata.

## CTX-02 circuit-breaker lineage

PR `#113` intentionally preserves the sequence of independent review failures that hardened the accepted capsule boundary:

- `#5274094804` — whole structured PA disposition surface could disappear;
- `#5274163927` — reopen/escalation minimum shape was not enforced causally;
- `#5274257119` — same-ID semantic substitution could survive compact validation;
- `#5274389905` — omission control did not exercise the real oracle and PA completeness used the wrong universe;
- `#5274609459` — audited index could still choose PA discovery/result selectors.

The accepted final candidate is `e5053b778e050cff83e2443fef888c64883c88ca`, independent PASS review `#5274937744`, implementation merge `af63528b63ba9b3ddf2e612c0ad8dff96a57a6c2`, followed by combined CTX-02/DW-plan DocSync persistence `107694d3850a478849bffd9510dc030910fc8aa3`.

The authoritative details remain the exact review submissions, accepted workpack, `Docs/engineering/CONTEXT_CAPSULE_V1.md`, CTX-02 evidence and live GitHub. This file intentionally does not restate every causal proof.

## CTX-03 history-separation rule

From CTX-03 onward:

1. current state/gate facts stay in the smallest authoritative/derived current-state surfaces needed by normal roles;
2. repeated closure chronology belongs under `Docs/history/` or exact evidence, not in normal bootstrap merely because it once mattered;
3. accepted evidence is never deleted for token savings;
4. historical pointers remain sufficient to reconstruct the original PR/review/candidate lineage;
5. DocSync updates the compact accepted-state projection once, then only affected authoritative docs; it does not copy the same transition into multiple current-state narratives.

`Docs/evidence/CTX-03/HISTORICAL_CLASSIFICATION.json` is the structured CTX-03 classification of this corpus. It is audit evidence only, not a semantic oracle.
