# WP-H1-05 — Managed scene projection + generational publication

Status: COMPLETE / ACCEPTED
Class: FOUNDATIONAL
EXECUTION_REQUIREMENT: LOCAL_UNITY_REQUIRED
Depends on: `WP-H1-04` PASS
Blocks: `WP-H1-06`

Acceptance: accepted candidate `186224fbc3f53eb9c47ae528a56dcf3514af163f`; PR `#192`; independent PASS review `#5308430565`; implementation merge `7039adecac4e6e07d899247759d9b3c9fd3c2dae`; Arkus Main Safety run `36038109465` GREEN; exact-SHA hosted verifier and validation-context jobs GREEN within Candidate Validation run `36038109560`; owner process-waiver comment `#5819709475` explicitly waived the fresh physical-local Unity receipt / Worker-handoff refreeze for this repair iteration without claiming that a new local execution occurred; post-acceptance DocSync `Docs/evidence/WP-H1-05/DOCSYNC.md`.

## Objective and central claim

Materialize canonical resources and Unity binding intent into a deterministic bridge-managed Unity scene graph, using staged generations so failure/interruption cannot replace the active projection or produce a false success receipt.

## WHY_THIS_BOUNDARY

This is the first effective write-projection claim: scene, hierarchy, GameObject/Transform mapping and publication. Prefab/asset fidelity and arbitrary component schemas can fail independently and remain later WPs. Removing staging/publication would leave no meaningful materialization claim, so those stay together.

## Inherited guarantees

H0 canonical identity/validation/provenance, H1-00 bridge transaction/receipt, H1-01 binding semantics, H1-03 authority, H1-03A public Editor execution/lifecycle and H1-04 identity/catalogue snapshots plus its accepted Quaternius Source baseline.

## New guarantees owned

- managed scene/root/object marker and mapping contract;
- normalized scene membership, hierarchy and Transform realization;
- versioned projection plan, materialize and normalized observe capabilities published through canonical composition and therefore reference/MCP;
- staged generation with one active manifest publication;
- idempotent same-input materialization and deletion/recreation handling;
- normalized effective scene observation anchored to canonical/catalogue inputs.

## Explicitly not re-proved

Canonical transaction/journal, catalogue completeness, H1-04 asset adoption/license decision, prefab/source-asset behavior, general component reflection, Unity validation breadth, art quality or crash/power-loss durability.

## Allowed scope

Unity scene creation in fixed managed roots, managed marker component, Transform adapter, generation staging/publication/cleanup, observation and local evidence. Game-representative content used by the positive proof may come from the accepted H1-04 Quaternius Source baseline.

## Forbidden scope

Writing unmanaged/user scenes, changing purchased upstream source assets/prefabs, canonical state mutation, gameplay components, live runtime state, navigation/physics, broad Cantabrian art conversion, wardrobe/prop/animation production or claiming byte-identical YAML as parity.

## Architecture / authority boundary

The active generation is chosen by the bridge manifest only after successful plan/apply/observe. Canonical state remains authoritative if no generation is active or materialization fails. Native object IDs are replaceable mapping details. Quaternius content is a project input resolved through the catalogue, never canonical authority.

## Acceptance criteria

- one canonical object maps to one managed object in the target scene and duplicate markers fail closed;
- canonical containment plus binding data produce deterministic hierarchy/transform under the documented coordinate convention;
- create/update/delete converge to the expected normalized graph;
- a second same-input materialization has no semantic delta;
- forced failure before publication leaves prior active generation and receipt unchanged;
- deleting generated output and rematerializing yields the same normalized observation digest;
- canonical hash/journal remain unchanged by materialization;
- scene save/reload and batchmode observation agree semantically;
- reference and MCP dispatch the same composed plan/materialize/observe handlers and return equivalent normalized outcomes; Editor-bound materialize/observe phases cross H1-03A, while a portable planning phase may remain in the external host.

## Deterministic proof / evidence

Materialize a Potes-shaped plaza/market/bar/workshop hierarchy, using accepted Quaternius Source content wherever the proof needs game-representative art and harness-only synthetic objects only where they are specifically needed for bridge mechanics/negative controls. Mutate canonical binding intent through H0, rematerialize, interrupt staging, reload the editor project and compare normalized observations. Independently enumerate managed markers from the effective scene.

## Causal negative-conformance classes

- one canonical object omitted or duplicated;
- parent/transform ignored;
- interrupted staging becomes active;
- success receipt emitted before effective observation;
- materializer changes canonical hash/journal;
- stale generation presented as current;
- second apply produces semantic churn;
- materialization or observation is reachable only through a private batch script/menu/test helper and absent from composed public discovery.

## Content-shape probe

Required. The approved hierarchy includes at least three levels, siblings, a deletion and a cross-reference so a flat cube fixture cannot satisfy the claim. Where visual/game content is needed, use the accepted H1-04 Quaternius Source baseline rather than fabricating disposable production substitutes.

## Dependency / IP implications

Consumes the pinned Unity baseline and the exact H1-04-approved Quaternius Source inputs. This WP does not adopt a competing art source and does not own art derivation. Harness-specific repository fixtures remain allowed for bridge-only proof mechanics and causal negative controls.

## Residual risks

Power-loss/torn-file durability, arbitrary scene size, real prefab relationship fidelity, render polish, Cantabrian adaptation, missing production assets and gameplay behavior remain unclaimed.

## Exact predecessor reopen condition

Reopen H1-00 if Unity cannot implement the accepted generation/receipt semantics without changing them. Ordinary Unity-specific locator/storage choices remain H1-05 implementation details. Reopen H1-04 only if the accepted source/catalogue identity is proven false or inapplicable.

## PASS consequence / next dependency

PASS unblocks `WP-H1-06`.
