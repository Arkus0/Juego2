# WP-DW-GATE — Design World second-consumer readiness gate

Status: PLANNED / NOT_STARTED
Class: FOUNDATIONAL GATE
EXECUTION_REQUIREMENT: REMOTE_OK
Depends on: `WP-DW-05` PASS + merge + DocSync
Blocks: final H2 external/public-boundary acceptance unless H2 explicitly reviews and dispositions the DW evidence/interlock

## Objective and central claim

Answer: has Arkus, through accepted public generic surfaces and without domain leakage or authority inversion, served two materially different real project consumers (CITY and PA), produced causal validation/query evidence, preserved provenance/losslessness, and demonstrated useful structured-context reduction while preserving actual paired-agent Worker/Reviewer-like correctness sufficiently to inform H2 boundary design?

## WHY_THIS_BOUNDARY

This is a closure/composition claim after all semantic owners exist. It may reconcile evidence, replay the bounded end-to-end scenarios and state H2 consequences, but it cannot invent missing provider, validation, retrieval or generic-boundary behavior to obtain PASS.

## Inherited guarantees

All accepted DW-00 through DW-05 plus the accepted H0 contracts they consume. H1-GATE remains an independent prerequisite/authority for Unity bridge readiness and is not re-proved here.

## New guarantees owned

- composed clean-rebuild CITY and PA second-consumer reference scenario;
- exhaustive DW stage/residual/limitation reconciliation;
- final bounded verdict on second-consumer architecture and structured-context utility;
- explicit statement of demonstrated versus non-demonstrated Arkus generality;
- durable H2 planning handoff with required/optional/rejected consequences and exact evidence anchors.

## Explicitly not re-proved

Every H0 suite, every predecessor negative matrix, H1 Unity parity, arbitrary-domain universality, external repository packaging/distribution, full CITY/PA correctness or future model performance.

## Allowed scope

Gate orchestration, clean rebuild/reference scenario, causal gate-owned omission controls, residual/limitation reconciliation, H2 handoff documentation and evidence.

## Forbidden scope

New DW semantics, kernel fixes, new domain schemas, changing predecessor oracles, Unity/gameplay implementation, workpack-process modeling, or using a plausible AI answer to repair missing deterministic evidence.

## Architecture / authority boundary

Gate begins from accepted CITY/PA authority anchors, rebuilds derived DW projections through the accepted generic contract, validates/queries them, verifies the accepted DW-04 pre-tuning task-selection freeze plus paired-agent quality/context evidence, and ends with evidence/handoff. Accepted sources remain authority throughout. Missing predecessor capability routes back to its causal owner.

## Acceptance criteria

- every DW-00..05 accepted stage required by the gate executes from a clean derived-state start on the exact candidate/evidence anchors;
- CITY projection rebuild is equal, its real invariant suite is GREEN on accepted truth, and gate-owned omission of one required stage/invariant execution turns the gate RED;
- PA projection rebuild is equal, its declared source universe/dispositions/fixtures/evidence relations reconcile exactly, and omission of one material accepted record/relation turns the gate RED;
- generic/domain-leakage audit remains GREEN and no unreviewed H0 semantic delta is present;
- the accepted DW-04 pre-tuning `TASK_SELECTION_FREEZE` chronology is intact: eligible universe/selection rule or exact reviewed manifest, task prompts, authority anchors, expected facts/blockers/verdicts, scorer and matched-run policy all predate route-specific tuning/results and were not silently rewritten afterward;
- the accepted DW-04 deterministic context-assembly layer still meets 100% required expected material/fallback reachability and its accepted context-reduction threshold under the frozen protocol;
- the accepted DW-04 paired agent executions exist for every selected task on both CTX and DW routes, use the same declared model/configuration/tool/run policy within each pair, and deterministically score 100% of required expected facts plus required causal blockers/verdicts with no verdict-changing false finding;
- all predecessor residuals/limitations are reconciled from an independent/effective evidence inventory and none can disappear by omission from the gate manifest;
- H2 handoff distinguishes: demonstrated public capabilities, discovered generic limitations, required H2 boundary considerations, optional downstream opportunities and explicit non-claims;
- H2 handoff preserves the existing external-consumer planning question rather than claiming in-repo DW evidence proves fresh-repository productization;
- `Docs/ROADMAP.md` still records the accepted DW sequence and conditional final-H2-boundary interlock consistently with the handoff;
- foundational proof is READY with zero unresolved in-boundary obligation/class.

## Deterministic proof / evidence

Use a clean checkout/derived-state deletion, exact accepted source anchors and exact DW schema/projection versions. Rebuild CITY and PA projections, compare normalized digests/results, execute invariant/query/losslessness suites, rerun deterministic DW-04 context assembly against the accepted frozen suite, and mechanically verify the accepted paired-agent result records against the immutable expected/scoring manifest and matched model/configuration evidence. Model prose is execution evidence, not the oracle; the deterministic expected/scoring manifest remains authoritative. Reconcile limitation/residual manifests and the H2/ROADMAP handoff.

The gate need not spend additional model runs merely for repetition if DW-04's accepted exact evidence remains valid and unchanged; it must fail if the required paired executions, scoring evidence or pre-tuning selection chronology are absent/inconsistent.

## Causal negative-conformance classes

- one mandatory gate stage removed while its declaration remains;
- one predecessor residual/limitation omitted from gate reconciliation;
- one accepted CITY source object/role omitted while the source universe remains;
- one PA disposition/fixture relation omitted while compact queries remain plausible;
- structured-context result is reported without counting source fallback/context bytes;
- one accepted DW-04 paired CTX or DW agent execution is removed while deterministic context assembly remains GREEN;
- one paired run's model/config/tool/run-policy evidence is changed so routes are no longer matched;
- the pre-tuning task-selection freeze is edited after result evidence without invalidating/restarting DW-04;
- a domain-specific shared dependency is hidden from the leakage audit;
- ROADMAP loses the accepted DW→final-H2-boundary interlock while lower-level docs still claim it;
- H2 handoff claims external packaging/universality not demonstrated by DW;
- gate attempts to repair a missing predecessor capability instead of routing it.

## Content-shape probe

Required and composed from the accepted DW-01/02 CITY universe, DW-03 PA universe and DW-05 neutral boundary probe. No new domain or broader corpus is introduced at the gate. DW-04 contributes its already accepted frozen task universe and paired-agent evidence; the gate does not choose a friendlier replacement suite.

## Dependency / IP implications

No new dependency or content adoption. Gate reconciles repository-owned evidence only.

## Residual risks

Even PASS leaves explicit residuals: arbitrary-domain universality, fresh external repository consumption, packaging/version compatibility, design↔Unity drift, real asset coverage/production cost, replay QA at gameplay scale, narrative knowledge validation and agent performance outside the exact frozen DW-04 task/model/configuration/run-policy universe.

## Exact predecessor reopen condition

A predecessor reopens only when effective gate evidence proves its accepted guarantee false/inapplicable. Missing gate wiring/evidence is gate-owned. Gate cannot fix either class.

## PASS consequence / next dependency

PASS authorizes the future H2 planner to treat DW's second-consumer evidence as accepted planning input. It does not itself authorize H2 implementation, external distribution or downstream DW opportunities. Final H2 public/external-boundary acceptance must explicitly incorporate or disposition both DW-GATE evidence and the existing H2 future-planning signal under the top-level `Docs/ROADMAP.md` interlock. FAIL routes to the causal DW owner or gate proof boundary.
